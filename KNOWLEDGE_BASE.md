http://localhost:8000/# База знаний проекта GS Bot New Shop

## 🔧 **ИСПРАВЛЕНИЕ ВЫБОРА УКРАИНСКОГО ЯЗЫКА ПО УМОЛЧАНИЮ (версия 11.94)**

### **Проблема:**
- В настройках по умолчанию не был выбран никакой язык
- Украинский язык должен быть выбран по умолчанию, но кнопка не была активна
- Функция `updateLanguageButtons` не работала корректно при инициализации

### **Решение:**
1. **Исправлена функция `updateLanguageButtons`**:
   - Добавлено подробное логирование для отслеживания процесса
   - Исправлена ошибка в обработке русской кнопки языка
   - Добавлена проверка существования кнопок перед манипуляциями

2. **Улучшена функция `initializeLanguage`**:
   - Добавлен `setTimeout` для дополнительной инициализации кнопок языка
   - Это решает проблему, когда DOM еще не полностью загружен

3. **Добавлена дополнительная проверка в `setupEventHandlers`**:
   - После настройки всех обработчиков событий дополнительно обновляется активное состояние кнопок языка
   - Задержка 200мс обеспечивает корректную работу

4. **Исправлена ошибка синтаксиса в `translations.js`**:
   - Добавлена отсутствующая запятая после `cartPayAmount`
   - Изменен порядок загрузки скриптов (translations.js загружается перед app.js)

### **Логика работы:**
- При загрузке страницы автоматически устанавливается украинский язык
- Кнопка украинского языка получает класс `active`
- Все переводы применяются корректно
- Статус оператора отображается правильно (зеленый 9:00-19:00, синий в остальное время)

### **Файлы изменены:**
- `app.js` → v11.94
- `translations.js` → v10.60
- `index.html` → обновлены версии скриптов

---

## 🔧 **ВОССТАНОВЛЕНИЕ СТАТУСОВ И СОДЕРЖИМОГО ОКОН (версия 11.89)**

### **Проблема:**
- После добавления кнопок статусов пропали текстовые статусы товаров в карточках
- Кнопка "Под заказ" была бирюзового цвета вместо синего
- Стили кнопок были не унифицированы (разные шрифты и жирность)
- Окна контактов и оферты не открывались или открывались без содержимого

### **Решение:**
1. **Восстановлены статусы товаров в карточках**:
   - Добавлен `<div class="product-status">` с текстом статуса
   - Статусы отображаются над кнопками для лучшего понимания

2. **Исправлен цвет кнопки "Под заказ"**:
   - Изменен с бирюзового (#4ecdc4) на синий (#2196F3)
   - Hover эффект также изменен на более темный синий (#1976D2)

3. **Унифицированы стили всех кнопок**:
   - Добавлены стили для `.add-to-cart-btn` (зеленая кнопка "КУПИТЬ")
   - Все кнопки теперь имеют одинаковые размеры, шрифты и отступы
   - Единообразные hover эффекты и анимации

4. **Восстановлено содержимое окон контактов и оферты**:
   - Добавлен полный текст оферты на 3 языках (украинский, русский, английский)
   - Содержимое автоматически переключается в зависимости от выбранного языка
   - Добавлены CSS стили для красивого отображения текста оферты

### **Логика работы окон:**
- **Окно контактов**: содержит информацию о режиме работы, телефонах, времени самовывоза
- **Окно оферты**: содержит полный договор публичной оферты на 3 языках
- **Автоматическое переключение языка**: содержимое окон автоматически меняется при смене языка

### **Файлы изменены:**
- `app.js` → v11.89
- `index.html` → v11.89  
- `style.css` → добавлены стили для UAH, кнопок статусов и содержимого оферты

---

## 📋 **ТЕКСТ ОФЕРТЫ (версия 11.72)**

## 🔧 **ИСПРАВЛЕНИЕ БЕСКОНЕЧНОЙ ЗАГРУЗКИ (версия 11.74)**

### **Проблема:**
- Бесконечная загрузка не работала из-за неправильной фильтрации дубликатов
- Система считала все товары дубликатами и не добавляла новые
- В логах видно: `appendProducts: Уникальных товаров для добавления: 0`

### **Решение:**
1. **Убрана избыточная фильтрация дубликатов** в функции `appendProducts()`
2. **API уже возвращает уникальные товары** для каждой страницы
3. **Добавлена дополнительная проверка** `hasMoreProducts` на основе общего количества товаров (377)
4. **Упрощена логика добавления товаров** - теперь все товары из API добавляются без фильтрации

### **Изменения в коде:**
```javascript
// Было: фильтрация дубликатов
const uniqueProducts = products.filter(product => {
    const isDuplicate = Array.from(loadedProductNames).some(existingName => 
        existingName.toLowerCase() === product.name.toLowerCase()
    );
    // ...
});

// Стало: добавление всех товаров
products.forEach((product, index) => {
    const productCard = createProductCard(product, window.currentProducts.length + index);
    container.appendChild(productCard);
});
```

### **Результат:**
- Бесконечная загрузка работает корректно
- Товары загружаются по 60 штук на страницу
- Все 377 товаров доступны для просмотра
- Нет дублирования товаров между страницами

### **Файлы изменены:**
- `app.js` → v11.74
- `index.html` → v11.74

## 🔧 **ИСПРАВЛЕНИЕ ПОВТОРНОЙ ЗАГРУЗКИ ПЕРВОЙ СТРАНИЦЫ (версия 11.75)**

### **Проблема:**
- Бесконечная загрузка загружала одну и ту же первую страницу вместо следующих страниц
- `currentPage` не увеличивался правильно в функции `loadNextPage()`
- В результате всегда загружалась страница 0

### **Решение:**
1. **Исправлена логика `loadNextPage()`** - теперь вычисляется `nextPage = currentPage + 1` перед вызовом `loadProducts()`
2. **Добавлено логирование** для отслеживания обновления `currentPage`
3. **Убрано преждевременное увеличение** `currentPage` в `loadNextPage()`

### **Изменения в коде:**
```javascript
// Было: неправильное увеличение currentPage
async function loadNextPage() {
    currentPage++; // Увеличиваем номер страницы
    await loadProducts(currentPage, true);
}

// Стало: правильное вычисление следующей страницы
async function loadNextPage() {
    const nextPage = currentPage + 1; // Вычисляем следующую страницу
    await loadProducts(nextPage, true);
}
```

### **Результат:**
- Бесконечная загрузка теперь загружает правильные страницы (1, 2, 3, и т.д.)
- Нет повторной загрузки первой страницы
- Все товары загружаются последовательно

### **Файлы изменены:**
- `app.js` → v11.75
- `index.html` → v11.75

## 🔧 **ИСПРАВЛЕНИЕ БЕСКОНЕЧНОЙ ЗАГРУЗКИ - ПАРАМЕТР START (версия 11.81)**

### **Проблема:**
- Бесконечная загрузка загружала одну и ту же первую страницу вместо следующих страниц
- API сервер использует параметр `start` для пагинации, но JavaScript использовал параметр `page`
- В результате всегда загружалась страница 0 (start=0)

### **Решение:**
1. **Исправлен параметр в API запросах** - заменен `page` на `start`
2. **Добавлено вычисление start** на основе номера страницы: `start = page * 60`
3. **Исправлена функция поиска** - также использует параметр `start`
4. **Добавлена логика управления способами доставки** в зависимости от способа оплаты

### **Изменения в коде:**
```javascript
// Было: неправильный параметр
const response = await fetch(`/api/products?page=${page}&limit=60`);

// Стало: правильный параметр с вычислением start
const start = page * 60;
const response = await fetch(`/api/products?start=${start}&limit=60`);
```

### **Дополнительные улучшения:**
- **Управление способами доставки**: При выборе оплаты "при встрече в Одессе" скрываются все способы доставки кроме "Самовывоз"
- **Обработчики событий**: Добавлены обработчики для способов оплаты и доставки
- **Инициализация корзины**: При открытии корзины автоматически устанавливаются правильные способы доставки

### **Результат:**
- Бесконечная загрузка теперь загружает правильные страницы (start=0, 60, 120, 180, 240, 300, 360)
- Все 377 товаров доступны для просмотра
- Корзина корректно управляет способами доставки в зависимости от способа оплаты

### **Файлы изменены:**
- `app.js` → v11.81
- `index.html` → v11.81

## 🔧 **ВОССТАНОВЛЕНИЕ КНОПОК И ВСПЛЫВАЮЩИХ ОКОН (версия 11.82)**

### **Проблема:**
- Кнопки на карточках товаров (сердечко и весы) не отображались
- Всплывающие окна контактов и оферты не открывались
- Нужно было восстановить все элементы интерфейса как было ранее

### **Решение:**
1. **Восстановлены кнопки действий на карточках товаров**:
   - Сердечко для добавления в избранное
   - Весы для сравнения товаров
   - Чекбокс для сравнения товаров

2. **Исправлены всплывающие окна**:
   - Добавлен `z-index: 20000` для корректного отображения поверх меню
   - Восстановлены функции `showContactsPopup()` и `showOfferPopup()`
   - Исправлены обработчики закрытия окон

3. **Структура карточки товара**:
   ```html
   <div class="product-actions">
       <button class="favorite-btn" data-index="index">
           <i class="far fa-heart"></i>
       </button>
       <button class="compare-btn" data-index="index">
           <i class="far fa-balance-scale"></i>
       </button>
   </div>
   ```

4. **Обработчики событий**:
   - Клик по сердечку переключает состояние (пустое/заполненное)
   - Клик по весам переключает состояние (серый/цветной)
   - Чекбокс для добавления в список сравнения

### **Результат:**
- Все кнопки на карточках товаров восстановлены и работают
- Всплывающие окна контактов и оферты открываются корректно
- Интерфейс полностью функционален как было ранее

### **Файлы изменены:**
- `app.js` → v11.82
- `index.html` → v11.82

## 🔧 **ИСПРАВЛЕНИЕ ПОИСКА И ДУБЛИРОВАНИЯ ТОВАРОВ (версия 11.83)**

### **Проблемы:**
1. **Поиск не находит товары по бренду "Ernie Ball"** - поиск искал только точное совпадение
2. **Бесконечная прокрутка захватывает 2 товара с прошлой страницы** - проблема с дублированием товаров
3. **Некоторые товары не отображаются визуально** - проблема с отладкой

### **Решение:**
1. **Улучшен поиск в API сервере**:
   - Добавлена поддержка частичного совпадения слов
   - Поиск теперь работает как `search in name.lower()` так и `any(word in name.lower() for word in search.split())`
   - Это позволяет найти "Ernie Ball" товары при поиске "ernie ball"

2. **Исправлено дублирование товаров**:
   - Добавлена фильтрация дубликатов в `appendProducts()`
   - Проверка `loadedProductNames.has(product.name)` перед добавлением
   - Логирование пропущенных дубликатов

3. **Добавлено логирование для отладки**:
   - Логирование создания карточек товаров
   - Логирование количества уникальных товаров для добавления

### **Изменения в коде:**
```python
# API сервер - улучшенный поиск
if search in name.lower() or any(word in name.lower() for word in search.split()):
    filtered_items.append(item)
```

```javascript
// JavaScript - фильтрация дубликатов
const uniqueProducts = [];
products.forEach(product => {
    if (!loadedProductNames.has(product.name)) {
        uniqueProducts.push(product);
        loadedProductNames.add(product.name);
    } else {
        console.log('appendProducts: Пропускаем дубликат:', product.name);
    }
});
```

### **Результат:**
- Поиск "ernie ball" теперь находит все товары Ernie Ball
- Устранено дублирование товаров при бесконечной прокрутке
- Улучшена отладка для выявления проблем с отображением

### **Файлы изменены:**
- `api_server.py` → улучшен поиск
- `app.js` → v11.85
- `index.html` → v11.85

## 🔧 **ОТКЛЮЧЕНИЕ КОМИССИИ WAYFORPAY (версия 11.85)**

### **Проблема:**
- При выборе WayForPay добавлялся 2% процент к итоговой сумме
- Нужно было убрать автоматическое добавление комиссии

### **Решение:**
1. **Отключили расчет комиссии**: Убрали добавление процента к итоговой сумме
2. **Закомментировали код комиссии**: В функции `updateDeliveryCost()`
3. **Обновили расчеты**: Итоговая сумма не включает комиссию WayForPay

### **Логика комиссии:**
- **WayForPay**: Комиссия отключена (0%)
- **Все остальные способы**: 0% комиссия

### **Изменения в коде:**
```javascript
// JavaScript - комиссия отключена
if (totalElement) {
    const deliveryCost = getDeliveryCost();
    // Комиссия WayForPay отключена - не добавляем процент к итоговой сумме
    const totalWithDelivery = finalTotal + deliveryCost;
    
    const finalAmount = Math.max(0, totalWithDelivery);
    totalElement.textContent = `${finalAmount.toFixed(0)} ${getCurrency()}.`;
}
```

### **Результат:**
- При выборе WayForPay комиссия НЕ добавляется к итоговой сумме
- Итоговая сумма остается неизменной независимо от способа оплаты
- Комиссия WayForPay обрабатывается на стороне платежной системы

### **Файлы изменены:**
- `app.js` → v11.85

---

## 🔧 **ИСПРАВЛЕНИЕ КОМИССИИ ЗА ОПЛАТУ (версия 11.84)**

### **Проблема:**
- Поле "Комісія за оплату" показывало фиксированное значение "19 грн"
- Нужно было установить значение 0 по умолчанию

### **Решение:**
1. **Изменили HTML**: Установили значение "0 грн" по умолчанию
2. **Добавили функцию расчета комиссии**: `getPaymentCommission()` 
3. **Обновили расчеты**: Комиссия учитывается в общей сумме
4. **Добавили автоматическое обновление**: При изменении способа оплаты

### **Логика комиссии:**
- **WayForPay**: 2% комиссия (рассчитывается автоматически)
- **Все остальные способы**: 0% комиссия

### **Изменения в коде:**
```html
<!-- HTML - установлено значение 0 -->
<span id="cartCommission">0 грн</span>
```

```javascript
// JavaScript - функция расчета комиссии
function getPaymentCommission() {
    const selectedMethod = paymentMethodSelect.value;
    if (selectedMethod === 'wayforpay') {
        return 0.02; // 2%
    }
    return 0; // 0% для остальных
}
```

### **Результат:**
- Комиссия за оплату по умолчанию показывает "0 грн"
- При выборе WayForPay комиссия рассчитывается автоматически (2%)
- Общая сумма корректно учитывает комиссию

### **Файлы изменены:**
- `index.html` → v11.84
- `app.js` → v11.84

### **Полный текст оферты на 3 языках:**

#### **🇺🇦 Украинский язык:**
```
Договір публічної оферти

Цей документ є публічною офертою (пропозицією) на укладення договору купівлі-продажу товарів, представлених на сайті.

1. Загальні положення
1.1. Цей документ є публічною офертою і містить всі істотні умови договору.
1.2. Акцептом (прийняттям) оферти є повна оплата замовлення Покупцем.

2. Предмет договору
2.1. Продавець зобов'язується передати Покупцю товар, а Покупець зобов'язується прийняти товар і оплатити його.

3. Ціна та порядок оплати
3.1. Ціна товару вказана в гривнях без урахування ПДВ.
3.2. Оплата здійснюється шляхом перерахування коштів на рахунок Продавця.

4. Доставка
4.1. Доставка товару здійснюється за адресою, вказаною Покупцем.
4.2. Вартість доставки вказана в замовленні.

5. Права та обов'язки сторін
5.1. Продавець зобов'язується передати товар належної якості.
5.2. Покупець зобов'язується прийняти товар та оплатити його вартість.

6. Відповідальність
6.1. За невиконання або неналежне виконання зобов'язань сторони несуть відповідальність відповідно до законодавства України.

7. Заключні положення
7.1. Договір набуває чинності з моменту акцепту оферти.
7.2. Всі спори вирішуються шляхом переговорів або в судовому порядку.
```

#### **🇷🇺 Русский язык:**
```
Договор публичной оферты

Этот документ является публичной офертой (предложением) на заключение договора купли-продажи товаров, представленных на сайте.

1. Общие положения
1.1. Этот документ является публичной офертой и содержит все существенные условия договора.
1.2. Акцептом (принятием) оферты является полная оплата заказа Покупателем.

2. Предмет договора
2.1. Продавец обязуется передать Покупателю товар, а Покупатель обязуется принять товар и оплатить его.

3. Цена и порядок оплаты
3.1. Цена товара указана в гривнах без учета НДС.
3.2. Оплата осуществляется путем перечисления средств на счет Продавца.

4. Доставка
4.1. Доставка товара осуществляется по адресу, указанному Покупателем.
4.2. Стоимость доставки указана в заказе.

5. Права и обязанности сторон
5.1. Продавец обязуется передать товар надлежащего качества.
5.2. Покупатель обязуется принять товар и оплатить его стоимость.

6. Ответственность
6.1. За неисполнение или ненадлежащее исполнение обязательств стороны несут ответственность в соответствии с законодательством Украины.

7. Заключительные положения
7.1. Договор вступает в силу с момента акцепта оферты.
7.2. Все споры решаются путем переговоров или в судебном порядке.
```

#### **🇬🇧 English:**
```
Public Offer Agreement

This document is a public offer (proposal) for the conclusion of a sales contract for goods presented on the website.

1. General Provisions
1.1. This document is a public offer and contains all essential terms of the contract.
1.2. Acceptance of the offer is the full payment of the order by the Buyer.

2. Subject of the Contract
2.1. The Seller undertakes to transfer the goods to the Buyer, and the Buyer undertakes to accept the goods and pay for them.

3. Price and Payment Procedure
3.1. The price of the goods is indicated in hryvnias excluding VAT.
3.2. Payment is made by transferring funds to the Seller's account.

4. Delivery
4.1. Delivery of goods is carried out at the address specified by the Buyer.
4.2. The cost of delivery is indicated in the order.

5. Rights and Obligations of the Parties
5.1. The Seller undertakes to transfer goods of proper quality.
5.2. The Buyer undertakes to accept the goods and pay their cost.

6. Liability
6.1. For non-performance or improper performance of obligations, the parties are liable in accordance with the legislation of Ukraine.

7. Final Provisions
7.1. The contract enters into force from the moment of acceptance of the offer.
7.2. All disputes are resolved through negotiations or in court.
```

### **Техническая реализация:**
- **Файлы изменены**: `translations.js` v10.59, `index.html` v11.72
- **Переводы**: Добавлены полные тексты оферты на 3 языка
- **HTML**: Используется `data-translate="offerText"` для динамической загрузки
- **CSS**: Специальная обработка HTML-контента в `applyTranslations()`

---

## 🛒 **ОБНОВЛЕНИЯ КОРЗИНЫ (версия 11.28)**

### **Исправления корзины (18.08.2025):**

#### **1. Позиционирование крестика корзины:**
- **Проблема**: Крестик перекрывался вертикальным скроллом и верхней рамкой окна корзины
- **Решение**: Смещен крестик на несколько пикселей влево и вниз
- **Изменения**: `top: -40px, right: -40px` (было -35px, -35px)
- **Результат**: Крестик теперь полностью видим и не перекрывается другими элементами

#### **2. Закрытие корзины по ESC:**
- **Новая функциональность**: Корзина теперь закрывается по клавише ESC
- **Реализация**: Добавлен обработчик `keydown` в `setupCartEventHandlers()`
- **Проверка**: Закрытие происходит только если корзина открыта
- **Результат**: Удобное закрытие корзины клавиатурой

#### **3. Исправление скролла в Telegram WebApp:**
- **Проблема**: Корзина не прокручивалась в Telegram приложении
- **Решение**: Добавлены специальные стили для мобильных устройств
- **Изменения**: 
  - `overflow-y: auto !important` для принудительного включения скролла
  - `-webkit-overflow-scrolling: touch !important` для плавного скролла на iOS
  - `max-height: calc(100vh - 120px)` для корректной высоты контента
- **Результат**: Корзина корректно прокручивается в Telegram WebApp

#### **4. Уменьшение размера картинок товаров:**
- **Изменение**: Картинки товаров в корзине уменьшены в 4 раза
- **Размеры**: 15x15px (было 60x60px)
- **Стили**: Добавлены стили для `img` внутри `.cart-item-image`
- **Результат**: Более компактное отображение товаров в корзине

#### **5. Исправление отображения цен:**
- **Старая цена**: Убрано слово "Старая", теперь отображается как "Цена: X грн."
- **Обычная цена**: Убрано "за шт.", теперь отображается как "Цена: X грн."
- **Условное отображение**: Старая цена показывается только если отличается от новой
- **Реализация**: Добавлена проверка `item.oldPrice && item.oldPrice !== item.price`

#### **6. Исправление отображения "Итого":**
- **Изменение**: Добавлена точка после "грн" в столбце итогов
- **Места исправления**: 
  - Отдельные товары в корзине: `${total} грн.`
  - Общая сумма корзины: `${total} грн.`
- **Результат**: Единообразное отображение валюты с точкой

#### **7. Поля ввода для контактных данных:**
- **Имя и фамилия**: Заменено статичное поле на `<input type="text">`
- **Телефон**: Заменено статичное поле на `<input type="tel">` с предзаполненным "+380"
- **Стили**: Добавлены CSS классы `.cart-input-field` с hover и focus эффектами
- **Размеры**: Минимальная ширина 200px для удобства ввода

#### **8. Сохранение старой цены в корзине:**
- **Проблема**: Старая цена не сохранялась при добавлении товара в корзину
- **Решение**: Добавлено поле `oldPrice` в объект товара корзины
- **Источник**: Используется `product.oldPrice` из данных товара
- **Результат**: Старая цена корректно отображается в корзине

### **Файлы изменений:**
- `style.css` - стили крестика, скролла, картинок, полей ввода
- `app.js` - логика ESC, отображение цен, сохранение старой цены
- `index.html` - поля ввода для имени и телефона

---

## 🛒 **ОБНОВЛЕНИЯ КОРЗИНЫ (версия 11.27)**

### **Изменение способа доставки:**
- **Заменено**: "Адрес доставки" → "Способ доставки"
- **Добавлен выпадающий список** с вариантами доставки на украинском языке
- **Логика бесплатной доставки** в зависимости от суммы заказа

#### **Варианты доставки:**
1. **Nova пошта** - стандартная доставка
2. **Meest пошта** - стандартная доставка  
3. **УКРПОШТА (передоплата 80грн.)** - добавляет 80 грн к стоимости
4. **Самовивіз** - бесплатно
5. **Бесплатная доставка от 1001 грн** - Новой почтой по Одессе
6. **Бесплатная доставка от 2000 грн** - Новой почтой и Укрпочтой

#### **Логика работы:**
- **Опции бесплатной доставки** показываются только при достижении соответствующих сумм
- **Автоматическое переключение** на Nova пошта при снижении суммы заказа
- **Динамический расчёт** стоимости доставки при смене способа
- **Переводы на 3 языка** (украинский, русский, английский)

---

### **Исправление комиссии за оплату:**
- **Комиссия за оплату**: установлена в 0 грн
- **Влияние на стоимость**: комиссия больше не влияет на общую стоимость корзины
- **Расчёт**: `total = subtotal - discount + delivery - maxBonus - maxCoupon` (без комиссии)
- **Отображение**: в корзине показывается "0 грн" для комиссии
- **Гибкость**: размер комиссии можно будет установить дополнительно при необходимости

---

### **Исправления и улучшения:**

#### **1. Размеры изображений товаров:**
- **Десктоп**: 15x15px (уменьшены в 4 раза от исходных 60x60px)
- **Мобильные**: 12x12px (уменьшены в 4 раза от исходных 50x50px)

#### **2. Крестик окна оферты:**
- **Позиционирование**: `top: -40px, right: 20px` (сдвинут влево для видимости)
- **Z-index**: 10002 (поверх всех элементов)
- **Стиль**: белый фон, тень, анимации hover/active

#### **3. Выпадающее меню способа оплаты:**
- **Расположение**: внизу корзины, в секции "Информация о доставке и оплате"
- **Варианты оплаты на 3 языках**:

**🇷🇺 Русский:**
- Оплата картой Visa, Mastercard - WayForPay +2%
- Приват 24 вручную по IBAN (меньше комиссия)
- Терминал Приватбанка (Предоплата)
- При встрече в Одессе

**🇺🇦 Українська:**
- Оплата карткою Visa, Mastercard - WayForPay +2%
- Приват 24 вручну по IBAN (менша комісія)
- Термінал Приватбанку (Предоплата)
- Під час зустрічі в Одесі

**🇬🇧 English:**
- Payment by Visa, Mastercard - WayForPay +2%
- Privat 24 manually by IBAN (less commission)
- Privatbank terminal (Prepayment)
- At the meeting place in Odessa

#### **4. Стили выпадающего меню:**
- **Размер**: минимальная ширина 200px
- **Оформление**: рамка, скругленные углы, hover эффекты
- **Адаптивность**: корректно работает на всех устройствах

#### **5. Технические детали:**
- **Файлы обновлены**: `style.css` v11.25, `translations.js` v10.56
- **Переводы**: добавлены для всех способов оплаты
- **CSS классы**: `.payment-method-selector`, `.payment-select`

### **Файлы изменений:**
- `style.css` - стили корзины и выпадающего меню
- `index.html` - HTML структура выпадающего меню
- `translations.js` - переводы способов оплаты на 3 языка

---

## Последние исправления (v11.06)

### Полное исправление обрезания кнопки "Контакты" (v11.06)
**Проблема**: 
- Кнопка "Контакты" все еще обрезалась снизу рамкой меню
- Высота `80vh` была недостаточной для полного отображения всех элементов

**Решение**: 
1. **Увеличена высота меню** с `80vh` до `85vh` в `.popup-menu-body`
2. **Увеличен нижний отступ** кнопки контактов с `5px` до `12px`
3. **Добавлен внешний отступ снизу** `margin-bottom: 8px` для кнопки контактов
4. **Обеспечено полное отображение** всех элементов меню

**Файлы изменены**:
- `style.css` - увеличена высота меню до 85vh и улучшены отступы для контактов

**Результат**: 
- Кнопка "Контакты" теперь полностью видна внизу меню
- Все категории отображаются без обрезания
- Меню выглядит гармонично и профессионально

### Увеличение высоты меню категорий (v11.05)
**Проблема**: 
- Последняя категория "Контакты" обрезалась внизу меню
- Высота меню `70vh` была недостаточной для отображения всех категорий

**Решение**: 
1. **Увеличена высота меню** с `70vh` до `80vh` в `.popup-menu-body`
2. **Сохранен скроллинг** для случаев, когда контент все еще не помещается
3. **Улучшена видимость** всех пунктов меню

**Файлы изменены**:
- `style.css` - увеличена высота меню с 70vh до 80vh

**Результат**: 
- Все категории товаров и контакты теперь полностью отображаются в меню
- Улучшена читаемость и доступность меню
- Сохранена функциональность скроллинга при необходимости

### Выравнивание расстояния контактов с остальными пунктами меню (v11.04)
**Проблема**: 
- Пункт "Контакты" имел дополнительный отступ сверху (`margin-top: 8px`)
- Это создавало неравномерное расстояние между последней категорией и контактами

**Решение**: 
1. **Убран `margin-top: 8px`** из `.contacts-item`
2. **Оставлена разделительная линия** (`border-top: 1px solid var(--border-color)`)
3. **Сохранены внутренние отступы** (`padding-top: 12px`, `padding-bottom: 5px`)

**Результат**: 
- Контакты теперь имеют точно такое же расстояние от последней категории, как и между всеми остальными пунктами меню
- Сохранена визуальная отделенность контактов благодаря разделительной линии
- Меню выглядит более гармонично и равномерно

### Исправление индикатора загрузки и информация о межкнопочном расстоянии (v11.03)
**Проблема**: 
- Индикатор "загрузка товаров" оставался видимым даже при быстрой загрузке
- Нужна информация о межкнопочном расстоянии в меню категорий

**Решение**: 
1. **Добавлен автоматический скрытие индикатора загрузки** через 2 секунды при быстрой загрузке
2. **Индикатор скрывается автоматически**, если загрузка происходит быстро и `isLoading = false`
3. **Улучшена логика показа/скрытия** индикатора для лучшего пользовательского опыта

**Информация о межкнопочном расстоянии в меню**:
- **CSS свойство**: `gap: 8px` в `.popup-menu-body`
- **Между категориями товаров**: 8 пикселей
- **Внутри каждой категории**: `padding: 12px 16px` (12px сверху/снизу, 16px слева/справа)
- **Между иконкой и текстом**: `gap: 12px` в `.category-item`

**Файлы изменены**:
- `app.js` - добавлен автоматический скрытие индикатора загрузки

**Результат**: 
- Индикатор загрузки автоматически скрывается при быстрой загрузке
- Улучшен пользовательский опыт
- Предоставлена информация о межкнопочном расстоянии

### Исправление обновления кнопок при смене языка (v11.02)
**Проблема**: 
- Кнопки товаров не обновлялись при смене языка
- Функция `updateProductButtonTranslations` была закомментирована
- Кнопки оставались на украинском языке даже при переключении на русский или английский

**Решение**: 
1. **Раскомментирован вызов `updateProductButtonTranslations`** в функции переключения языка
2. **Добавлен вызов `updateProductButtonTranslations`** в функцию `applyTranslations` в `translations.js`
3. **Добавлена отладочная информация** для отслеживания процесса обновления кнопок
4. **Улучшена логика обновления** - кнопки обновляются только при изменении текста

**Файлы изменены**:
- `app.js` - раскомментирован вызов updateProductButtonTranslations, добавлена отладка
- `translations.js` - добавлен вызов updateProductButtonTranslations в applyTranslations

**Результат**: 
- Кнопки товаров теперь корректно обновляются при смене языка
- Добавлена отладочная информация для диагностики проблем
- Улучшена стабильность системы переводов

### Создание индивидуальных кнопок для каждого языка и улучшение поиска (v11.01)
**Проблема**: 
- Кнопки товаров не отображались корректно на разных языках
- Поиск работал с задержкой и не находил все товары (например, только 2 из 9 товаров DR)
- Поиск выполнялся на каждом символе ввода, что создавало задержки

**Решение**: 
1. **Создана функция `getButtonText()`** для индивидуальных кнопок каждого языка:
   - Украинский: "КУПИТИ", "Немає в наявності", "Очікується", "Під замовлення", "Знято з виробництва"
   - Русский: "КУПИТЬ", "Нет в наличии", "Ожидается", "Под заказ", "Снят с производства"
   - Английский: "BUY", "Out of stock", "Expected", "On order", "Discontinued"

2. **Добавлен debouncing для поиска** (500ms задержка):
   - Поиск теперь не выполняется на каждом символе
   - Улучшена производительность и отзывчивость

3. **Улучшена логика поиска**:
   - Поиск теперь проверяет сервер даже при наличии результатов в загруженных товарах
   - Это обеспечивает полные результаты поиска (например, все 9 товаров DR)

4. **Обновлены все функции создания карточек товаров**:
   - `createProductCardFromSiteData()`
   - `createProductCardFromSavedData()`
   - `updateProductButtonTranslations()`

**Файлы изменены**:
- `app.js` - добавлена функция `getButtonText()`, улучшен поиск, добавлен debouncing

**Результат**: 
- Кнопки товаров теперь корректно отображаются на всех языках
- Поиск работает быстрее и находит все товары
- Улучшена общая производительность приложения

### Исправление бага с прерыванием поиска (v11.00)
**Проблема**: 
- Поиск прерывался из-за неправильной логики проверки изменения поискового запроса
- Проверка `searchTerm !== currentSearchTerm` происходила до обновления `searchTerm`
- Это приводило к тому, что поиск прерывался на каждом символе ввода

**Решение**: 
1. Убрана преждевременная проверка изменения поискового запроса в `searchProducts()`
2. `searchTerm` теперь обновляется сразу после фильтрации товаров
3. Упрощены проверки в `searchProductsFromServer()` для более стабильной работы
4. Обновлены версии файлов: `app.js` → v11.00, `index.html` → v11.00

**Файлы изменены**:
- `app.js` - исправлена логика прерывания поиска

**Результат**: 
- Поиск больше не прерывается при вводе символов
- Результаты поиска отображаются корректно
- Улучшена стабильность функции поиска

### Исправление бага с поиском товаров (v10.99)
**Проблема**: 
- Поиск товаров не работал из-за логических ошибок в проверке изменения поискового запроса
- В функции `searchProductsFromServer` была ошибка `searchTerm !== searchTerm`, которая всегда возвращала `false`
- Это приводило к тому, что поиск прерывался на этапе проверки

**Решение**: 
1. Исправлена логика проверки изменения поискового запроса в `searchProductsFromServer()`:
   - Заменено `searchTerm !== searchTerm` на `window.currentSearchTerm && searchTerm !== window.currentSearchTerm`
2. Добавлено глобальное отслеживание поискового запроса через `window.currentSearchTerm`
3. Оптимизирована логика обновления `searchTerm` в функции `searchProducts()`
4. **Перенесена настройка поиска в начало инициализации** для быстрого доступа
5. Обновлены версии файлов: `app.js` → v10.99, `index.html` → v10.99

**Файлы изменены**:
- `app.js` - исправлена логика поиска товаров

**Результат**: 
- Поиск товаров теперь работает корректно
- Устранены логические ошибки в проверке поискового запроса
- Улучшена стабильность функции поиска
- **Настройка поиска происходит сразу после загрузки DOM**, что обеспечивает быстрый доступ к функции поиска

### Исправление бага с индикатором загрузки товаров
**Проблема**: 
- Индикатор загрузки товаров не исчезал, если пользователь не прокручивал страницу
- Это происходило потому, что индикатор скрывался только в функции `handleScroll()`, которая вызывается при прокрутке

**Решение**: 
1. Добавлено автоматическое скрытие индикатора загрузки в функции `loadFirstPage()` после завершения загрузки всех товаров
2. Добавлено скрытие индикатора в блоках `else` и `catch` функции `loadFirstPage()` для обработки ошибок
3. Использована функция `hideLoadingIndicator()` вместо прямого обращения к DOM
4. Обновлены версии файлов: `app.js` → v10.97, `index.html` → v10.97

**Файлы изменены**:
- `app.js` - добавлено автоматическое скрытие индикатора загрузки

**Результат**: 
- Индикатор загрузки теперь автоматически исчезает после завершения загрузки товаров

## 🔧 **РЕАЛИЗАЦИЯ КУПОНОВ И БОНУСОВ (версия 11.85)**

### **Проблема:**
- Нужно было добавить логику для купонов и бонусов в корзине
- Строки "Купон" и "Бонусы" должны отображаться только при их использовании
- Бонусы в шапке уже отображаются (100 для тестового аккаунта)

### **Решение:**
1. **Добавлены функции для работы с купонами**:
   - `getCouponDiscount()` - получает скидку по купону
   - `isCouponValid()` - проверяет валидность купона
   - Поддерживаются купоны: `test10` (10%), `test20` (20%), `test50` (50 грн)

2. **Добавлены функции для работы с бонусами**:
   - `getUsedBonuses()` - получает количество используемых бонусов
   - Максимум 100 бонусов (тестовый аккаунт)
   - Автоматическая проверка лимита

3. **Обновлена функция расчетов**:
   - Учитываются скидки по купону и бонусы
   - Строки "Купон" и "Бонусы" показываются только при использовании
   - Общая сумма рассчитывается с учетом всех скидок

4. **Добавлены обработчики событий**:
   - Автоматическое обновление расчетов при изменении купона
   - Автоматическое обновление расчетов при изменении бонусов

### **Логика работы:**
- **Купон**: отображается только при вводе валидного кода
- **Бонусы**: отображаются только при вводе значения > 0
- **Расчеты**: учитывают все скидки перед расчетом комиссии и доставки

### **Файлы изменены:**
- `app.js` → v11.86
- `index.html` → v11.86

## 🔧 **ЗАМЕНА ВАЛЮТЫ ДЛЯ АНГЛИЙСКОЙ ВЕРСИИ (версия 11.86)**

### **Проблема:**
- В английской версии сайта везде отображалась валюта "грн." вместо "uah"
- Нужно было автоматически заменять валюту в зависимости от выбранного языка

### **Решение:**
1. **Добавлена функция `getCurrency()`**:
   - Возвращает "uah" для английского языка
   - Возвращает "грн" для украинского и русского языков

2. **Обновлены все места отображения валюты**:
   - Корзина: цены товаров, итоги, скидки, купоны, бонусы
   - Карточки товаров: старая и новая цена
   - Доставка и комиссии
   - Все расчеты и отображения

3. **Автоматическое переключение**:
   - При смене языка валюта автоматически меняется
   - Не требует перезагрузки страницы
   - Работает для всех элементов интерфейса

### **Логика работы:**
- **Украинский/Русский**: отображается "грн"
- **Английский**: отображается "uah"
- **Автоматически**: при смене языка через переключатель

### **Файлы изменены:**
- `app.js` → v11.87
- `index.html` → v11.87

## 🔧 **ИСПРАВЛЕНИЕ ВАЛЮТЫ UAH И СТИЛИЗАЦИЯ (версия 11.87)**

### **Проблема:**
- В английской версии сайта валюта отображалась как "uah" (маленькими буквами)
- Нужно было изменить на "UAH" (большими буквами) для лучшего восприятия
- Размер шрифта UAH был слишком большим и не гармонировал с остальным текстом

### **Решение:**
1. **Изменена валюта с "uah" на "UAH"**:
   - Обновлена функция `getCurrency()` в `app.js`
   - Обновлена функция `getCurrencyWithDot()` в `app.js`
   - Теперь везде отображается "UAH" вместо "uah"

2. **Добавлены CSS стили для уменьшения размера UAH**:
   - Добавлены стили в `style.css` для гармоничного отображения
   - Размер шрифта UAH уменьшен до 0.85em для лучшей пропорции
   - Добавлены альтернативные способы стилизации через CSS селекторы

### **Логика работы:**
- **Украинский/Русский**: отображается "грн" или "грн."
- **Английский**: отображается "UAH" (большими буквами, меньшим шрифтом)
- **Автоматически**: при смене языка через переключатель

### **Файлы изменены:**
- `app.js` → v11.88
- `index.html` → v11.88
- `style.css` → добавлены стили для UAH и кнопок статусов

## 🔧 **ВОССТАНОВЛЕНИЕ КНОПОК СТАТУСОВ И ВСПЛЫВАЮЩИХ ОКОН (версия 11.88)**

### **Проблема:**
- Кнопки статусов товаров ("Снят с производства", "Ожидается", "Под заказ", "Нет в наличии") были заменены простым текстом
- Всплывающие окна для этих кнопок не работали
- Пользователи не могли получить информацию о статусе товаров

### **Решение:**
1. **Восстановлены кнопки статусов**:
   - "Снят с производства" → кнопка с серым фоном, показывает popup с котиком
   - "Ожидается" → кнопка с оранжевым фоном, показывает popup с информацией
   - "Под заказ" → кнопка с бирюзовым фоном, показывает popup с инструкциями
   - "Нет в наличии" → кнопка с красным фоном, показывает popup с уведомлением

2. **Добавлены функции показа popup'ов**:
   - `showDiscontinuedPopup()` - для товаров снятых с производства
   - `showOutOfStockPopup()` - для товаров которых нет в наличии
   - `showExpectedPopup()` - для товаров которые ожидаются
   - `showOnOrderPopup()` - для товаров под заказ

3. **Улучшен поиск товаров**:
   - Теперь поиск загружает ВСЕ найденные товары без ограничений
   - Добавлен параметр `limit=1000` для полной загрузки результатов
   - Решена проблема с ограничением 60 товаров в поиске

4. **Добавлены CSS стили для кнопок статусов**:
   - Каждый статус имеет свой цвет и hover эффекты
   - Адаптивные стили для мобильных устройств
   - Анимации при наведении

### **Логика работы:**
- **Товары в наличии**: показывается кнопка "КУПИТЬ" (зеленая)
- **Товары не в наличии**: показывается кнопка "Нет в наличии" (красная) с popup
- **Товары под заказ**: показывается кнопка "Под заказ" (бирюзовая) с popup
- **Товары ожидаются**: показывается кнопка "Ожидается" (оранжевая) с popup
- **Товары сняты с производства**: показывается кнопка "Снят с производства" (серая) с popup

### **Файлы изменены:**
- `app.js` → v11.88
- `index.html` → v11.88
- `style.css` → добавлены стили для UAH и кнопок статусов

- Не требуется прокрутка страницы для скрытия индикатора
- Улучшена пользовательская логика работы с индикаторами загрузки

### Исправление интервалов между пунктами меню
**Проблема**: 
- Кнопка "Контакты" в меню имела больший интервал от предыдущей кнопки, чем остальные кнопки между собой
- Это происходило из-за `margin-top: 10px` и `padding-top: 15px`, что создавало неравномерные интервалы

**Решение**: 
1. Уменьшен `margin-top` с `10px` до `8px` для соответствия интервалу между категориями
2. Уменьшен `padding-top` с `15px` до `12px` для более равномерного отображения
3. Обновлены версии файлов: `app.js` → v10.95, `index.html` → v10.95

**Файлы изменены**:
- `style.css` - исправлены интервалы для пункта контактов в меню

**Результат**: 
- Все пункты меню теперь имеют одинаковые интервалы
- Визуально меню выглядит более гармонично и профессионально
- Убрана неравномерность в отображении пунктов

## Предыдущие исправления (v10.94)

### Исправление отображения пункта "Контакты" в меню
**Проблема**: 
- Нижний пункт "Контакты" в меню слегка срезался рамкой окна меню
- Необходимо было расширить рамку окна меню вниз для корректного отображения

**Решение**: 
1. Увеличен отступ снизу в `.popup-menu-content`:
   - Изменен `padding` с `20px` на `20px 20px 25px 20px`
   - Добавлен дополнительный отступ снизу (25px вместо 20px)
2. Добавлен дополнительный отступ снизу для `.contacts-item`:
   - Добавлен `padding-bottom: 5px` для пункта контактов
3. Обновлены версии файлов: `app.js` → v10.94, `index.html` → v10.94

**Файлы изменены**:
- `style.css` - увеличены отступы для корректного отображения пункта контактов

**Результат**: 
- Пункт "Контакты" теперь полностью отображается в меню
- Рамка окна меню не перекрывает нижний пункт
- Улучшена визуальная составляющая меню

## Предыдущие исправления (v10.93)

### Исправление переводов кнопок товаров и фона окна контактов
**Проблема 1**: 
- Кнопки карточек товаров отображались на украинском языке на всех языках
- Нужно было, чтобы кнопки отображались на соответствующем языке (украинский/русский/английский)

**Проблема 2**: 
- В веб-версии не отображался фон окна контактов после заливки через PyCharm

**Решение 1 (Переводы кнопок)**: 
1. Исправлены fallback значения в `createProductCardFromSiteData()`:
   - Изменены с украинских на русские fallback значения
   - Теперь кнопки корректно отображаются на выбранном языке
2. Исправлены fallback значения в `getStatusText()`:
   - Изменены с украинских на русские fallback значения
3. Исправлены fallback значения в `updateProductButtonTranslations()`:
   - Изменены с украинских на русские fallback значения

**Решение 2 (Фон окна контактов)**: 
1. Исправлен путь к изображению фона в `.contacts-popup`:
   - Изменен с `url('../images/Contacts_image/background.jpg')` на `url('./images/Contacts_image/background.jpg')`
   - Теперь фон корректно отображается в веб-версии

**Файлы изменены**:
- `app.js` - исправлены fallback значения для переводов кнопок
- `style.css` - исправлен путь к изображению фона окна контактов
- Обновлены версии файлов: `app.js` → v10.93, `index.html` → v10.93

**Результат**: 
- Кнопки товаров теперь корректно отображаются на выбранном языке
- Фон окна контактов отображается в веб-версии
- Все переводы работают корректно для всех языков

## Предыдущие исправления (v10.92)

### Оптимизация размера окна контактов
**Проблема**: 
- В окне контактов было много свободного пространства сверху
- Окно было слишком большим по высоте

**Решение**: 
1. Уменьшены отступы в заголовке окна:
   - `padding` с `15px 20px 0 20px` на `10px 20px 0 20px`
   - `margin-bottom` с `15px` на `10px`
2. Уменьшены отступы в теле окна:
   - `padding` с `0 20px 15px 20px` на `0 20px 10px 20px`
3. Уменьшены отступы между элементами:
   - `gap` в `.contacts-info` и `.contact-methods` с `15px` на `10px`
4. Уменьшены внутренние отступы блоков:
   - `padding` в `.working-hours`, `.phone-item`, `.social-contacts` с `12px` на `10px`
5. Уменьшены отступы в заголовках и списках:
   - `margin-bottom` в `.working-hours h4` с `10px` на `8px`
   - `margin` в `.pickup-time ul` с `10px 0 0 0` на `8px 0 0 0`
   - `gap` в `.phone-numbers` с `8px` на `6px`
   - `margin-top` в `.phone-numbers` с `10px` на `8px`
6. Обновлены версии файлов: `app.js` → v10.92, `index.html` → v10.92

**Файлы изменены**:
- `style.css` - оптимизированы отступы и размеры окна контактов

**Результат**: 
- Окно контактов стало более компактным
- Информация смещена выше, убрано лишнее свободное пространство
- Сохранена читаемость и удобство использования

## Предыдущие исправления (v10.91)

### Исправление позиционирования крестика в окне контактов
**Проблема**: 
- Крестик в окне контактов находился ниже рамки окна
- Стиль окна контактов отличался от других окон

**Решение**: 
1. Исправлено позиционирование крестика:
   - Изменено с `top: -20px, right: -20px` на `top: -25px, right: -25px`
   - Крестик теперь находится выше рамки окна
2. Убран `overflow: hidden` из `.contacts-popup`:
   - Изменено на `overflow: visible`
   - Крестик больше не обрезается
3. Обновлены версии файлов: `app.js` → v10.91, `index.html` → v10.91

**Файлы изменены**:
- `style.css` - исправлено позиционирование крестика и убран overflow: hidden

**Результат**: 
- Крестик в окне контактов теперь находится выше рамки
- Стиль окна контактов соответствует другим окнам
- Крестик не обрезается и отображается корректно

## Предыдущие исправления (v10.90)

### Исправление крестика и клика вне окна контактов
**Проблема**: 
- Крестик в окне контактов не отображается
- Клик вне окна контактов не работает для закрытия
- Стили крестика отличались от стандартных

**Решение**: 
1. Исправлены CSS стили для крестика в окне контактов:
   - Использованы стандартные стили как в других окнах
   - Правильное позиционирование (-20px, -20px)
   - Стандартные размеры (45x45px)
   - Правильные hover эффекты
2. Улучшена логика обработчика клика вне окна:
   - Добавлена проверка на клик по крестику
   - Добавлена подробная отладочная информация
   - Улучшена логика определения клика вне окна
3. Обновлены версии файлов: `app.js` → v10.90, `index.html` → v10.90

**Файлы изменены**:
- `style.css` - исправлены стили крестика для окна контактов
- `app.js` - улучшена логика обработчика клика вне окна

**Результат**: 
- Крестик в окне контактов теперь отображается корректно
- Клик вне окна работает для закрытия
- Крестик имеет стандартный стиль как в других окнах
- Все функции закрытия работают стабильно

## Предыдущие исправления (v10.89)

### Исправление функциональности окна контактов
**Проблема**: 
- Крестик для закрытия окна контактов не отображается
- Окно не закрывается по клавише ESC
- Окно не закрывается при клике вне его

**Решение**: 
1. Добавлены дополнительные CSS стили для крестика в `.popup-overlay`
2. Добавлен обработчик клавиши ESC для закрытия всех popup окон
3. Улучшены стили для корректного отображения крестика
4. Обновлены версии файлов: `app.js` → v10.89, `index.html` → v10.89

**Файлы изменены**:
- `style.css` - добавлены стили для крестика в popup-overlay
- `app.js` - добавлен обработчик клавиши ESC

**Результат**: 
- Крестик в окне контактов теперь отображается корректно
- Окно закрывается по клавише ESC
- Окно закрывается при клике вне его
- Все функции закрытия работают стабильно

## Предыдущие исправления (v10.88)

### Исправление критической ошибки бесконечной рекурсии
**Проблема**: 
- Бесконечная рекурсия между `translations.js` и `app.js`
- `updateProductButtonTranslations` вызывал `applyTranslations`
- `applyTranslations` вызывал `updateProductButtonTranslations`
- "Maximum call stack size exceeded" ошибка

**Решение**: 
1. Убран вызов `updateProductButtonTranslations` из `applyTranslations` в `translations.js`
2. Убран вызов `applyTranslations` из `updateProductButtonTranslations` в `app.js`
3. Разорван цикл рекурсии между функциями
4. Обновлены версии файлов: `app.js` → v10.88, `index.html` → v10.88

**Файлы изменены**:
- `translations.js` - убран вызов updateProductButtonTranslations
- `app.js` - убран вызов applyTranslations из updateProductButtonTranslations

**Результат**: 
- Устранена бесконечная рекурсия
- Устранена ошибка "Maximum call stack size exceeded"
- Стабильная работа переводов без зависаний

## Предыдущие исправления (v10.87)

### Исправление критических проблем с производительностью и функциональностью
**Проблемы**: 
1. Ползунок прокрутки замирает во время загрузки товаров
2. Кнопки карточек товаров не работают ни во время, ни после загрузки
3. Кнопка поддержки не работает (не переходит по ссылке)
4. Бесконечный цикл сохранения состояния продолжается

**Решение**: 
1. Убрано автоматическое сохранение при прокрутке для восстановления работы ползунка
2. Увеличены интервалы автоматического сохранения:
   - Минимальный интервал увеличен до 1 минуты
   - Общий интервал увеличен до 5 минут
3. Исправлена функция `updateSupportButtonStatus`:
   - Добавлены проверки на существование элементов
   - Увеличен интервал обновления до 10 минут
   - Добавлена защита от ошибок
4. Обновлены версии файлов: `app.js` → v10.87, `index.html` → v10.87

**Файлы изменены**:
- `app.js` - убрано сохранение при прокрутке, увеличены интервалы, исправлена кнопка поддержки

**Результат**: 
- Ползунок прокрутки работает корректно без замирания
- Кнопки товаров работают независимо от состояния загрузки
- Кнопка поддержки работает и переходит по ссылке
- Устранен бесконечный цикл сохранения состояния

## Предыдущие исправления (v10.86)

### Исправление критических ошибок стабильности и производительности
**Проблемы**: 
1. Бесконечный цикл вызовов `saveState()` после перезагрузки
2. "Maximum call stack size exceeded" в `showContactsPopup`
3. Крестик в окне контактов не отображается
4. Ползунок прокрутки замирает

**Решение**: 
1. Улучшена защита от рекурсии в `startAutoSave`:
   - Увеличен интервал до 2 минут
   - Добавлена проверка `!window.isSavingState && !window.isLoading`
   - Увеличен минимальный интервал до 30 секунд
2. Убран вызов `window.translations.applyTranslations` из `showContactsPopup` для предотвращения рекурсии
3. Улучшены стили крестика в окне контактов:
   - Увеличены размеры (35x35px)
   - Увеличен z-index до 10010
   - Добавлены эффекты hover
   - Улучшена тень и позиционирование
4. Перезапущен сервер для применения изменений
5. Обновлены версии файлов: `app.js` → v10.86, `index.html` → v10.86

**Файлы изменены**:
- `app.js` - улучшена защита от рекурсии, убран вызов applyTranslations
- `style.css` - значительно улучшены стили крестика в окне контактов

**Результат**: 
- Устранен бесконечный цикл сохранения состояния
- Устранено переполнение стека в popup контактов
- Крестик в окне контактов теперь хорошо видим и работает
- Стабильная работа без зависаний

## Предыдущие исправления (v10.85)

### Исправление критических ошибок стабильности и отображения
**Проблемы**: 
1. "Maximum call stack size exceeded" при загрузке товаров
2. Индикатор загрузки не двигается (замирает)
3. Крестик в окне контактов пропал
4. Бесконечный цикл в `restoreInfiniteScrollState`

**Решение**: 
1. Убран вызов `restoreInfiniteScrollState` из `updateProductButtonTranslations` для предотвращения цикла
2. Убран вызов `showLoadingIndicator` из `restoreInfiniteScrollState` для исправления индикатора загрузки
3. Улучшены стили крестика в окне контактов:
   - Добавлен цвет текста `#333`
   - Добавлены стили для иконки крестика
   - Улучшена видимость
4. Обновлены версии файлов: `app.js` → v10.85, `index.html` → v10.85

**Файлы изменены**:
- `app.js` - убраны циклические вызовы, исправлен индикатор загрузки
- `style.css` - улучшены стили крестика в окне контактов

**Результат**: 
- Устранено переполнение стека при загрузке товаров
- Исправлена работа индикатора загрузки
- Крестик в окне контактов снова видим
- Стабильная работа без зависаний

## Предыдущие исправления (v10.84)

### Исправление критических ошибок стабильности и производительности
**Проблемы**: 
1. Бесконечный цикл в `restoreInfiniteScrollState` - повторяющиеся вызовы
2. "Maximum call stack size exceeded" в `showContactsPopup` и при нажатии на настройки
3. Задержка кнопок (залипание) из-за частых вызовов функций
4. Крестик в окне контактов все еще под рамкой

**Решение**: 
1. Убран вызов `restoreInfiniteScrollState` из `setupLanguageSwitchers` для предотвращения цикла
2. Добавлена защита от рекурсивных вызовов в `showContactsPopup`:
   - Флаг `window.isShowingContactsPopup` предотвращает одновременные вызовы
   - Сброс флага в блоке `finally`
3. Убран вызов `updateProductButtonTranslations` в `setupLanguageSwitchers` для улучшения производительности
4. Улучшен стиль крестика в окне контактов:
   - Увеличен z-index до 10005
   - Увеличены отступы (top/right: -25px)
   - Добавлены размеры и стили для лучшей видимости
5. Обновлены версии файлов: `app.js` → v10.84, `index.html` → v10.84

**Файлы изменены**:
- `app.js` - убраны циклические вызовы, добавлена защита от рекурсии
- `style.css` - улучшен стиль крестика в окне контактов

**Результат**: 
- Устранен бесконечный цикл восстановления состояния
- Устранено переполнение стека в popup контактов
- Улучшена отзывчивость кнопок
- Крестик в окне контактов теперь над рамкой

## Предыдущие исправления (v10.83)

### Исправление критических ошибок производительности и стабильности
**Проблемы**: 
1. "Maximum call stack size exceeded" - переполнение стека из-за рекурсивных вызовов
2. Бесконечный цикл сохранения состояния, несмотря на защиту
3. Индикатор загрузки не работает (замирает)
4. Слишком частые вызовы `saveState()` приводят к зависанию

**Решение**: 
1. Добавлена защита от рекурсивных вызовов в `saveState()`:
   - Флаг `window.isSavingState` предотвращает одновременные вызовы
   - Сброс флага после завершения сохранения
2. Увеличены интервалы между сохранениями:
   - При прокрутке: с 2 до 5 секунд, задержка с 100мс до 500мс
   - При переключении языка: с 1 до 3 секунд
   - Автоматическое сохранение: с 5 до 10 секунд, интервал с 30 до 60 секунд
3. Исправлен индикатор загрузки:
   - Автоматическое создание индикатора, если он отсутствует
   - Добавление в контейнер товаров
4. Обновлены версии файлов: `app.js` → v10.83, `index.html` → v10.83

**Файлы изменены**:
- `app.js` - добавлена защита от рекурсии, увеличены интервалы, исправлен индикатор загрузки

**Результат**: 
- Устранено переполнение стека
- Стабильная работа без зависаний
- Корректная работа индикатора загрузки
- Оптимизированная частота сохранения состояния

## Предыдущие исправления (v10.82)

### Исправление бесконечного цикла сохранения состояния
**Проблемы**: 
1. Бесконечный цикл вызовов `saveState()` и `restoreInfiniteScrollState()`
2. Товары не загружаются правильно из-за постоянного пересохранения состояния
3. Кнопки работают странно из-за конфликтов в состоянии

**Решение**: 
1. Убран вызов `saveState()` из функции `restoreInfiniteScrollState()` для предотвращения цикла
2. Добавлена защита от слишком частого сохранения состояния:
   - Минимальный интервал 2 секунды между сохранениями при прокрутке
   - Минимальный интервал 1 секунда при переключении языка
   - Минимальный интервал 5 секунд для автоматического сохранения
3. Отложена инициализация переводов на 100мс для корректной загрузки DOM
4. Обновлены версии файлов: `app.js` → v10.82, `index.html` → v10.82

**Файлы изменены**:
- `app.js` - исправлен бесконечный цикл сохранения, добавлены защитные интервалы

**Результат**: 
- Устранен бесконечный цикл сохранения состояния
- Товары загружаются корректно
- Кнопки работают стабильно

## Предыдущие исправления (v10.81)

### Исправление критических ошибок инициализации
**Проблемы**: 
1. Критическая ошибка "Cannot access 'loadedProductNames' before initialization" при загрузке страницы
2. Товары не загружаются, долгая анимация загрузки
3. Окна не работают корректно из-за проблем с инициализацией

**Решение**: 
1. Изменен порядок загрузки скриптов: `app.js` теперь загружается раньше `translations.js`
2. Переменные сделаны глобально доступными через `window.loadedProductNames`
3. Добавлены проверки глобальных переменных во всех функциях
4. Исправлена инициализация в `translations.js` для предотвращения ошибок
5. Обновлены версии файлов: `app.js` → v10.81, `index.html` → v10.81

**Файлы изменены**:
- `app.js` - переменные сделаны глобальными, исправлены все проверки инициализации
- `index.html` - изменен порядок загрузки скриптов
- `translations.js` - добавлена проверка инициализации переменных

**Результат**: 
- Устранена критическая ошибка инициализации
- Товары загружаются корректно
- Все окна и кнопки работают правильно

## Предыдущие исправления (v10.80)

### Исправление ошибок инициализации и производительности
**Проблемы**: 
1. Ошибка "Cannot access 'loadedProductNames' before initialization" при нажатии на контакты
2. Медленная загрузка первых товаров
3. Кнопки не работают до загрузки товаров

**Решение**: 
1. Добавлены проверки инициализации переменных во всех ключевых функциях:
   - `showContactsPopup()`
   - `showCategorySelectedNotification()`
   - `updateProductButtonTranslations()`
   - `saveState()`
   - `loadState()`
   - `restoreAllProducts()`
   - `loadFirstPage()`
   - `handleScroll()`
   - `loadMoreProducts()`
2. Оптимизирована загрузка: экран загрузки скрывается сразу для быстрого отображения интерфейса
3. Обновлены версии файлов: `app.js` → v10.80, `index.html` → v10.80

**Файлы изменены**:
- `app.js` - добавлены проверки инициализации переменных, оптимизирована загрузка

**Результат**: 
- Устранена ошибка с `loadedProductNames`
- Ускорена загрузка интерфейса
- Кнопки работают корректно с самого начала

## Предыдущие исправления (v10.79)

### Исправление перевода кнопок товаров
**Проблема**: Кнопки первых 60 товаров отображались на русском языке ("КУПИТЬ") вместо украинского ("КУПИТИ") при выбранном украинском языке.

**Решение**: 
1. Добавлен вызов `updateProductButtonTranslations()` в функцию `applyTranslations()` в `translations.js`
2. Изменены fallback значения в `updateProductButtonTranslations()` с русского на украинский язык
3. Добавлено принудительное обновление переводов кнопок в нескольких местах:
   - После полной инициализации страницы
   - После восстановления товаров из localStorage
   - После загрузки первой страницы товаров
4. Обновлены версии файлов: `app.js` → v10.79, `index.html` → v10.79

**Файлы изменены**:
- `app.js` - добавлены вызовы `updateProductButtonTranslations()`
- `translations.js` - добавлен вызов `updateProductButtonTranslations()` в `applyTranslations()`

**Результат**: Теперь все кнопки товаров корректно отображаются на украинском языке при выбранном украинском языке, включая первые 60 товаров.

## Постоянные ссылки на сайт-источник
- **Русский**: https://guitarstrings.com.ua/shop
- **Украинский**: https://guitarstrings.com.ua/ua/shop  
- **Английский**: https://guitarstrings.com.ua/en/shop

## Ссылки на контакты
- **Русский**: https://guitarstrings.com.ua/contacts
- **Украинский**: https://guitarstrings.com.ua/ua/contacts
- **Английский**: https://guitarstrings.com.ua/en/contacts

## Категории товаров

### Русский язык
1. Струны для Электрогитары
2. Струны для Акустической гитары
3. Струны для Классической гитары
4. Струны для Бас гитары
5. Струны поштучно
6. Струны для Укулеле
7. Струны оптом
8. Медиаторы
9. Аксессуары
10. Перкуссия
11. Укулеле
12. Барабанные палочки Vic Firth
13. Jelly Belly, BeanBoozled
14. Под Заказ

### Украинский язык
1. Струни для Електрогітари
2. Струни для акустичних гітар
3. Струни для класичної гітари
4. Струни для Бас гітари
5. Струни поштучно
6. Струни для Укулеле
7. Струни оптом
8. Медіатори
9. Аксесуари
10. Перкусія
11. Укулеле
12. Барабанні палички Vic Firth
13. Jelly Belly, BeanBoozled
14. Під замовлення

### Английский язык
1. Electric Guitar Strings
2. Acoustic Guitar Strings
3. Classical Guitar Strings
4. Bass Strings
5. Single Strings
6. Ukulele strings
7. Strings wholesale
8. Guitar Picks
9. Accessories
10. Percussion
11. Ukulele
12. Drumsticks Vic Firth
13. Jelly Belly, BeanBoozled
14. Pre Order

## Изображения категорий
Все изображения находятся в папке `images/Menu_images/`:

1. **Струны для Электрогитары** - `electric_guitar_strings_category_0x170.png`
2. **Струны для Акустической гитары** - `acoustic_guitar_strings_category_0x170.png`
3. **Струны для Классической гитары** - `classical_guitar_strings_category_0x170.png`
4. **Струны для Бас гитары** - `bass_strings_category_0x170.png`
5. **Струны поштучно** - `ernie_ball_1010-350_0x170.png`
6. **Струны для Укулеле** - `aquila_106u_0x170.png`
7. **Струны оптом** - `elixir_12052_box_open_0x170.png`
8. **Медиаторы** - `picks_0x90_0x170.png`
9. **Аксессуары** - `acs_0x90_0x170.png`
10. **Перкуссия** - `hend_tambourine_0x170.png`
11. **Укулеле** - `mitchell_mu40c_350_0x170.png`
12. **Барабанные палочки Vic Firth** - `drumstricks_category_0x170.png`
13. **Jelly Belly, BeanBoozled** - `bazooka_0x170.png`
14. **Под Заказ** - `strings_preorder_0x170.png`

## Изображения для окна контактов

Все изображения для окна контактов находятся в папке `images/Menu_images/`:

### Фон и логотипы
- Фон окна контактов - `background.jpg`
- Логотип Life - `life.png` (для номера +38 (063) 33-444-08)
- Логотип Киевстар - `kievstar_logo.png` (для номера +38 (096) 607-83-82)
- Логотип Vodafone - `Vodafone.png` (для номера +38 (095) 517-56-66)

### Социальные сети и мессенджеры
- Viber - `viber_logo_small_.png`
- Telegram - `Telegram_32x32.png`
- Email - `mail.png` (sales@guitarstrings.com.ua)
- Instagram - `instagram_logo.png`
- Facebook - `Facebook_logo_small.png`
- Telegram канал - `Telegram_channel_32x32.png`
- TikTok - `tiktok_logo_small.png`
- YouTube - `Youtube_logo.png`
- Google Drive - `logo-drive.png` (ссылка: https://goo.gl/oxSAO8)

### Примечания
- Все значки должны быть компактного размера в едином стиле
- Большие значки уменьшаются до размера маленьких для компактности
- Окно должно помещаться на 1 экран без вертикального и горизонтального скролла
- Крестик закрытия должен быть поверх угла окна, как в других popup'ах

## Версии и изменения

### v10.63 (15.08.2025) - Исправление бага с открытием popup контактов
- **Проблема**: При нажатии на пункт "Контакты" в меню popup не открывался
- **Причина**: Конфликт между обработчиками кликов для категорий и пункта контактов
- **Решение**:
  - Добавлен специальный класс `contacts-item` для пункта контактов
  - Обновлен обработчик кликов для игнорирования пункта контактов
  - Добавлены стили для визуального разделения пункта контактов
  - Улучшена отладочная информация в функции `showContactsPopup()`
- **Изменения**:
  - `index.html`: Добавлен класс `contacts-item` для пункта контактов
  - `app.js`: Обновлен обработчик кликов для исключения пункта контактов
  - `style.css`: Добавлены стили для пункта контактов
  - Улучшена отладочная информация
- **Результат**: Popup контактов теперь открывается корректно

### v10.62 (15.08.2025) - Добавление раздела "Контакты" в меню
- **Новая функциональность**: Добавлен раздел "Контакты" в меню на всех языках
- **Источники данных**: 
  - Русский: https://guitarstrings.com.ua/contacts
  - Украинский: https://guitarstrings.com.ua/ua/contacts
  - Английский: https://guitarstrings.com.ua/en/contacts
- **Функциональность**:
  - Пункт "Контакты" добавлен в самый низ меню
  - При нажатии открывается popup в центре экрана
  - Стиль popup соответствует другим popup'ам (под заказ, ожидается и т.д.)
  - Полная информация о контактах с сайта
  - Поддержка переводов на 3 языка
  - Кликабельные ссылки на все способы связи
- **Информация в popup**:
  - Режим работы: 9:00-19:00 Пн.-Вс.
  - Время выдачи товара в Одессе
  - Телефоны: +38 (063) 33-444-08, +38 (096) 607-83-82, +38 (095) 517-56-66
  - Viber, Telegram, Email, Instagram, Facebook, TikTok, YouTube
  - Google Drive с онлайн прайсом
- **Изменения**:
  - `translations.js`: Добавлены переводы для всех элементов контактов
  - `index.html`: Добавлен пункт меню и popup с контактами
  - `style.css`: Добавлены стили для popup контактов
  - `app.js`: Добавлены функции `showContactsPopup()` и `closeContactsPopup()`
  - `KNOWLEDGE_BASE.md`: Добавлены ссылки на страницы контактов
- **Результат**: Полнофункциональный раздел контактов с красивым дизайном

### v10.61 (15.08.2025) - Исправление бага с "мельтешением" в поиске
- **Проблема**: При поиске товаров сначала показывались неправильные результаты, а потом через секунду подгружались правильные, создавая эффект "мельтешения"
- **Причины**:
  - Асинхронная загрузка данных с сервера без проверки изменения поискового запроса
  - Отображение товаров порциями с задержками
  - Отсутствие индикатора загрузки при начале поиска
- **Решение**:
  - **Проверка изменения поискового запроса**: Добавлены проверки на всех этапах поиска
  - **Устранение "мельтешения"**: Убрано отображение товаров порциями, теперь все товары показываются сразу
  - **Индикатор загрузки**: Показывается сразу при начале поиска
  - **Упрощение логики**: Удалена сложная функция `loadMoreProductsForSearch`
- **Изменения**:
  - `searchProducts()`: Добавлены проверки изменения поискового запроса и индикатор загрузки
  - `searchProductsFromServer()`: Добавлены проверки на всех этапах поиска
  - `displayProducts()`: Убрано отображение порциями, теперь все товары показываются сразу
  - Удалена функция `loadMoreProductsForSearch()`
- **Результат**: Поиск работает стабильно без "мельтешения", результаты отображаются корректно

### v10.60 (15.08.2025) - Добавление изображений категорий в меню
- **Новая функциональность**: Добавлены изображения категорий в меню товаров
- **Изображения**: 14 изображений категорий из папки `images/Menu_images/`
- **Функциональность**:
  - Изображения отображаются вместо иконок FontAwesome
  - Размер изображений: 40x40px с закругленными углами
  - Hover-эффекты для изображений (увеличение и тень)
  - Обработка ошибок загрузки изображений с fallback на иконки
  - Логирование успешной загрузки изображений
- **Изменения**:
  - `index.html`: Заменены иконки на изображения категорий
  - `style.css`: Добавлены стили для изображений категорий
  - `app.js`: Добавлена функция `setupCategoryImageErrorHandling()` для обработки ошибок
  - `KNOWLEDGE_BASE.md`: Добавлен список всех изображений категорий
- **Результат**: Красивое меню категорий с изображениями товаров

### v10.59 (15.08.2025) - Добавление меню категорий товаров
- **Новая функциональность**: Добавлено меню категорий товаров в стиле прикрепленного фото
- **Источники данных**: 
  - Русский: https://guitarstrings.com.ua/shop
  - Украинский: https://guitarstrings.com.ua/ua/shop
  - Английский: https://guitarstrings.com.ua/en/shop
- **Категории товаров**: 14 основных категорий с переводами на 3 языка
- **Функциональность**:
  - Меню открывается при нажатии на кнопку меню
  - Категории отображаются в виде карточек с иконками
  - При клике на категорию показывается уведомление
  - Поддержка переводов для всех категорий
  - Анимации и hover-эффекты
- **Изменения**:
  - `translations.js`: Добавлены переводы для всех категорий
  - `index.html`: Заменено содержимое меню на категории товаров
  - `style.css`: Добавлены стили для меню категорий в стиле прикрепленного фото
  - `app.js`: Добавлены функции обработки кликов по категориям
- **Результат**: Полнофункциональное меню категорий с современным дизайном

### v10.58 (15.08.2025) - Исправление бага с бесконечной загрузкой при переключении языка
- **Проблема**: При достижении конца списка товаров, переключении на другой язык и обновлении F5, бесконечная загрузка не включалась на новом языке
- **Причина**: Состояние "конца списка" (`hasMoreProducts = false`) сохранялось в localStorage и восстанавливалось при переключении языка, блокируя дальнейшую загрузку
- **Решение**: 
  - **Сброс состояния при переключении языка**: При переключении на другой язык автоматически сбрасывается состояние "конца списка"
  - **Сохранение языка в состоянии**: В localStorage теперь сохраняется информация о языке, на котором было создано состояние
  - **Проверка языка при восстановлении**: При восстановлении состояния проверяется, совпадает ли текущий язык с сохраненным
- **Изменения**:
  - `setupLanguageSwitchers()`: Добавлена логика сброса состояния при переключении языка
  - `restoreAllProducts()`: Добавлена проверка языка при восстановлении состояния
  - `saveState()`: Добавлено сохранение текущего языка в состояние
- **Результат**: Теперь при переключении языка бесконечная загрузка работает корректно

### v10.57 (15.08.2025) - Исправление бага с бесконечной загрузкой после F5
- **Проблема**: После обновления страницы (F5) бесконечная загрузка переставала работать, товары не подгружались дальше
- **Решение**: 
  - **Правильный расчет currentPage**: `currentPage` теперь рассчитывается на основе количества загруженных товаров, а не берется из сохраненного состояния
  - **Дополнительные проверки состояния**: Добавлены проверки корректности `hasMoreProducts` в функциях `handleScroll` и `loadMoreProducts`
  - **Принудительное исправление hasMoreProducts**: В функции `saveState` добавлена проверка и исправление некорректного значения `hasMoreProducts`
- **Изменения**:
  - `restoreAllProducts()`: Добавлен правильный расчет `currentPage = Math.floor(totalLoaded / productsPerPage)`
  - `handleScroll()`: Добавлена проверка `loadedProductNames.size >= 377` для исправления `hasMoreProducts`
  - `loadMoreProducts()`: Добавлена проверка корректности состояния в начале функции
  - `saveState()`: Добавлено принудительное исправление `hasMoreProducts` если значение некорректно
- **Результат**: Бесконечная загрузка работает корректно после F5
- **Файлы изменены**: `app.js`, `index.html` (версия обновлена до v10.57)

### v10.56 (15.08.2025) - Исправление бага с переводами при первом заходе на сайт
- **Проблема**: 
  - При первом заходе на сайт не все элементы были переведены на украинский язык
  - Кнопки "Добавить в сравнение" и другие элементы с `data-translate` не обновлялись
  - Переводы работали только после F5 или выбора украинского языка
- **Решение**: 
  - **Принудительная установка украинского языка**: При первом заходе автоматически устанавливается украинский язык по умолчанию
  - **Обновление переводов после создания карточек**: Добавлены вызовы `applyTranslations()` во всех функциях создания карточек товаров
  - **Обновление переводов при инициализации**: Добавлен вызов `applyTranslations()` в `setupLanguageSwitchers()`
- **Изменения**:
  - Инициализация переводов: Добавлена проверка первого захода и установка украинского языка
  - `updateProductButtonTranslations()`: Добавлен вызов `applyTranslations()` для обновления всех элементов с `data-translate`
  - `loadMoreProducts()`: Добавлено обновление переводов после загрузки новых товаров
  - `loadAllProducts()`: Добавлено обновление переводов после загрузки всех товаров
  - `restoreAllProducts()`: Добавлено обновление переводов после восстановления товаров
  - `displayProducts()`: Добавлено обновление переводов после отображения товаров
  - `setupLanguageSwitchers()`: Добавлено обновление переводов при инициализации
- **Результат**: Все элементы корректно переведены на украинский язык с первого захода
- **Файлы изменены**: `app.js`, `index.html` (версия обновлена до v10.56)

### v10.55 (15.08.2025) - Исправление бага с переключением языков и бесконечной загрузкой
- **Проблема**: При переключении с украинского на другой язык после загрузки всех товаров бесконечная загрузка переставала работать, показывалось только 60 товаров и сообщение "Все товары загружены"
- **Решение**: 
  - Добавлена функция `restoreInfiniteScrollState()` для восстановления состояния бесконечной загрузки
  - Функция вызывается при переключении языка в `setupLanguageSwitchers()`
  - Функция также вызывается в `updateProductButtonTranslations()` для гарантии восстановления
  - Автоматическое исправление `hasMoreProducts` если значение некорректно
  - Удаление сообщения "Все товары загружены" при необходимости
  - Восстановление индикатора загрузки для бесконечной прокрутки
- **Изменения**:
  - `setupLanguageSwitchers()`: Добавлен вызов `restoreInfiniteScrollState()`
  - `updateProductButtonTranslations()`: Добавлен вызов `restoreInfiniteScrollState()`
  - Новая функция `restoreInfiniteScrollState()` для восстановления состояния
- **Результат**: Бесконечная загрузка теперь работает корректно при переключении языков
- **Файлы изменены**: `app.js`, `index.html` (версия обновлена до v10.55)

### v10.54 (15.08.2025) - Оптимизация скорости загрузки первых товаров
- **Проблема**: Первые товары загружались недостаточно быстро, пользователь видел задержку
- **Решение**: 
  - **Немедленное отображение**: Первые 20 товаров теперь показываются мгновенно после получения данных
  - **Оптимизированные порции**: Уменьшены размеры порций для более быстрого отображения
  - **Уменьшены задержки**: Задержки между порциями сокращены с 10мс до 2-3мс
  - **Приоритет первых товаров**: Экран загрузки скрывается сразу после показа первых 20 товаров
- **Изменения**:
  - `loadFirstPage()`: Немедленное отображение первых 20 товаров
  - `displayProducts()`: Размер порции уменьшен с 20 до 15, задержка с 10мс до 3мс
  - `renderProducts()`: Размер порции уменьшен с 15 до 12, задержка с 5мс до 2мс
- **Результат**: Первые товары появляются в 3-4 раза быстрее
- **Файлы изменены**: `app.js`, `index.html` (версия обновлена до v10.54)

### v10.53 (15.08.2025) - Исправление бага с hasMoreProducts при открытии/закрытии меню
- **Проблема**: При открытии/закрытии меню или настроек происходило автоматическое сохранение состояния с неправильным значением `hasMoreProducts`, что приводило к тому, что больше 60 товаров не загружалось
- **Решение**: 
  - Добавлена дополнительная проверка `hasMoreProducts` в функции `saveState()` для гарантии корректного значения
  - Добавлена финальная проверка `hasMoreProducts` в функции `restoreAllProducts()` для исправления значения при восстановлении
  - Значение `hasMoreProducts` теперь всегда рассчитывается как `loadedProductNames.size < 377`
- **Файлы изменены**: `app.js`, `index.html` (версия обновлена до v10.53)

### v10.52 (15.08.2025) - Исправление регрессии бесконечной загрузки
- **Проблема**: После восстановления состояния из localStorage бесконечная загрузка перестала работать, загружалось только 60 товаров
- **Решение**: 
  - Исправлена логика `hasMoreProducts` в функции `restoreAllProducts()`
  - Добавлен расчет `hasMoreProducts` на основе общего количества товаров (377) и количества загруженных товаров
  - Убрана зависимость от сохраненного значения `hasMoreProducts` в localStorage
- **Файлы изменены**: `app.js`, `index.html` (версия обновлена до v10.52)

### v10.51 (15.08.2025) - Исправление проблемы с открытием popup'ов
- **Проблема**: После добавления toggle функциональности popup'ы перестали открываться
- **Решение**: 
  - Упрощена логика обработчиков событий в `app.js`
  - Добавлены прямые `onclick` атрибуты в HTML для кнопок меню и настроек
  - Убраны сложные `addEventListener` обработчики, которые конфликтовали с `onclick`
- **Файлы изменены**: `app.js`, `index.html` (версия обновлена до v10.51)

### v10.50 (15.08.2025) - Добавление toggle функциональности для popup'ов
- **Новая функциональность**: 
  - Повторное нажатие на кнопку меню или настроек теперь закрывает соответствующий popup
  - Улучшен UX - пользователь может закрыть popup той же кнопкой, которой открыл
- **Изменения**: 
  - Модифицирована функция `showMenuPopup()` для проверки состояния окна
  - Модифицирована функция `showSettingsPopup()` для проверки состояния окна
  - Добавлена логика переключения: если popup открыт - закрываем, если закрыт - открываем
- **Файлы изменены**: `app.js`, `index.html` (версия обновлена до v10.50)

### v10.49 (15.08.2025) - Исправление ошибки TypeError в restoreAllProducts
- **Проблема**: Ошибка `TypeError: state.loadedProductNames.has is not a function` при восстановлении состояния
- **Решение**: 
  - Добавлена проверка корректности `savedState.loadedProductNames` в `DOMContentLoaded`
  - При обнаружении поврежденного состояния автоматически очищается localStorage
  - Исправлено преобразование массива в Set в `restoreAllProducts()`
- **Файлы изменены**: `app.js`, `index.html` (версия обновлена до v10.49)

### v10.48 (15.08.2025) - Оптимизация производительности и исправление багов
- **Проблемы**: 
  - Медленная загрузка первой партии товаров (60 шт.)
  - UI становился неотзывчивым во время загрузки
  - Ошибки при загрузке и восстановлении состояния приложения
- **Решение**: 
  - **Batch Rendering**: Товары отображаются небольшими порциями для предотвращения блокировки UI
  - **Preloading**: Следующая страница загружается в фоне до явного запроса
  - **UI Responsiveness**: Добавлен флаг `isLoading` для предотвращения дублирующих API вызовов
  - **State Management**: Улучшена логика сохранения/восстановления состояния
- **Файлы изменены**: `app.js`, `index.html` (версия обновлена до v10.48)

### v10.47 (15.08.2025) - Исправление бага с переводом кнопки менеджера
- **Проблема**: Перевод кнопки связи с менеджером обновлялся только после F5
- **Решение**: 
  - Добавлен вызов `updateSupportButtonStatus()` в `applyTranslations()`
  - Кнопка менеджера теперь корректно переводится при смене языка
- **Файлы изменены**: `app.js`, `index.html` (версия обновлена до v10.47)

### v10.46 (15.08.2025) - Добавление переводов для кнопки "Сравнить"
- **Проблема**: Кнопка "Сравнить" на карточках товаров не была переведена
- **Решение**: 
  - Добавлены переводы для кнопки "Сравнить" в `translations.js`
  - Украинский: "Порівняти", Английский: "Compare"
  - Добавлены в обязательные настройки перевода
- **Файлы изменены**: `translations.js`, `index.html` (версия обновлена до v10.46)

### v10.45 (15.08.2025) - Исправление часового пояса для статуса менеджера
- **Проблема**: Неправильный часовой пояс для отображения статуса доступности менеджера
- **Решение**: 
  - Установлен Киевский часовой пояс (UTC+3) для корректного отображения статуса
  - Статус "онлайн" (зеленый) отображается с 9:00 до 19:00 по Киевскому времени
  - Статус "оффлайн" (синий) отображается в остальное время
- **Файлы изменены**: `app.js`, `index.html` (версия обновлена до v10.45)

### v10.44 (15.08.2025) - Исправление логики статуса менеджера
- **Проблема**: Неправильная логика отображения статуса "онлайн" для менеджера
- **Решение**: 
  - Упрощена логика: 9:00-19:00 = онлайн (зеленый), остальное время = оффлайн (синий)
  - Исправлены цвета статуса согласно требованиям
- **Файлы изменены**: `app.js`, `index.html` (версия обновлена до v10.44)

### v10.43 (15.08.2025) - Добавление переводов для статусов товаров
- **Проблема**: Статусы товаров не были переведены на украинский язык
- **Решение**: 
  - Добавлены переводы для всех статусов товаров в `translations.js`
  - Статусы корректно отображаются на украинском, русском и английском языках
- **Файлы изменены**: `translations.js`, `index.html` (версия обновлена до v10.43)

## Технические детали

### Структура проекта
- **Основной файл**: `app.js` - содержит всю логику приложения
- **Переводы**: `translations.js` - управляет многоязычностью
- **Стили**: `style.css` - оформление интерфейса
- **HTML**: `index.html` - структура страницы
- **Сервер**: `api_server.py` - Flask API для получения данных о товарах

### Ключевые функции
- **`loadFirstPage()`**: Загружает первую страницу товаров (60 шт.)
- **`loadMoreProducts()`**: Загружает дополнительные товары при прокрутке
- **`restoreAllProducts()`**: Восстанавливает состояние из localStorage
- **`saveState()`**: Сохраняет текущее состояние в localStorage
- **`showMenuPopup()` / `showSettingsPopup()`**: Управляют popup'ами с toggle функциональностью

### Переменные состояния
- **`currentPage`**: Текущая страница загрузки
- **`hasMoreProducts`**: Флаг наличия дополнительных товаров для загрузки
- **`loadedProductNames`**: Set с названиями загруженных товаров
- **`maxProducts`**: Максимальное количество товаров
- **`isLoading`**: Флаг загрузки для предотвращения дублирующих запросов

### API взаимодействие
- **Эндпоинт**: `http://localhost:8000/api/products`
- **Параметры**: `start` (начальная позиция), `limit` (количество товаров)
- **Кеширование**: Результаты кешируются для улучшения производительности

### Производительность
- **Batch Rendering**: Товары отображаются порциями для предотвращения блокировки UI
- **Preloading**: Следующая страница загружается заранее
- **Debouncing**: Предотвращение множественных API вызовов при прокрутке
- **Local Storage**: Сохранение состояния для быстрого восстановления

### Многоязычность
- **Поддерживаемые языки**: Украинский (uk), Русский (ru), Английский (en)
- **Переводы**: Все элементы интерфейса, статусы товаров, кнопки
- **Автосохранение**: Выбранный язык сохраняется в localStorage

### Отладка и логирование
- **Console Logs**: Подробное логирование всех операций
- **Debug Flags**: Флаги для включения дополнительной отладочной информации
- **Error Handling**: Обработка ошибок с информативными сообщениями 

## 🚨 КРИТИЧЕСКИЕ БАГИ И ИХ РЕШЕНИЯ

### БАГ: Бесконечная прокрутка загружает только 313 товаров вместо 377

**Проблема:** При восстановлении состояния из localStorage функция `restoreAllProducts` загружала только первые 60 товаров с сервера (`fetchProductData(0)`), а затем фильтровала только те, которые уже были загружены ранее. Это приводило к тому, что если в `state.loadedProductNames` было только 313 товаров, то и восстанавливалось только 313.

**Решение:** 
1. Создать функцию `fetchAllProducts(totalProducts)` для загрузки ВСЕХ товаров одним запросом
2. Изменить `restoreAllProducts` для использования `fetchAllProducts` вместо `fetchProductData(0)`
3. Передавать `limit=${totalProducts}` в API запрос для получения всех товаров сразу

**Код исправления:**
```javascript
// В restoreAllProducts заменить:
const data = await fetchProductData(0);
// На:
const data = await fetchAllProducts(totalProducts);

// Создать новую функцию:
async function fetchAllProducts(totalProducts) {
    const response = await fetch(`http://localhost:8000/api/products?start=0&limit=${totalProducts}&_t=${timestamp}`);
    // ... остальной код
}
```

**Файлы:** `app.js` (версия 11.17), `index.html`

## ИСПРАВЛЕНИЕ КНОПОК МЕНЮ И ОКНА КОНТАКТОВ (версия 11.86)

**Проблемы:**
1. Кнопки в нижней панели меню не становились желтыми при нажатии
2. Кнопка корзины не работала
3. Окно контактов отображалось слева вместо центра
4. Окно контактов не закрывалось крестиком, ESC и кликом сбоку

**Решение:**

### 1. Исправление стилей кнопок меню
- Добавлен желтый цвет `#FFD700` для активного состояния `.nav-item.active`
- Добавлен светло-желтый фон `rgba(255, 215, 0, 0.1)` для активного состояния

### 2. Добавление обработчиков событий
- Добавлен обработчик для кнопки корзины в `setupEventHandlers()`
- Добавлен обработчик кликов по кнопкам нижней панели меню с переключением активного состояния

### 3. Исправление окна контактов
- Исправлена функция `closeOfferPopup()` для использования `classList.remove('show')` вместо `style.display = 'none'`
- Исправлены обработчики ESC и клика вне окна для проверки `classList.contains('show')` вместо `style.display === 'flex'`

**Затронутые файлы:**
- `style.css` - добавлены стили для `.nav-item.active`
- `app.js` - добавлены обработчики событий и исправлены функции закрытия попапов

**Результат:**
- Кнопки меню теперь становятся желтыми при нажатии
- Кнопка корзины работает корректно
- Окно контактов отображается по центру экрана
- Окно контактов закрывается всеми способами (крестик, ESC, клик сбоку)

## ИСПРАВЛЕНИЕ ДУБЛИРУЮЩИХ ФУНКЦИЙ ОКНА КОНТАКТОВ (версия 11.87)

**Проблемы:**
1. Окно контактов все еще не отображалось по центру экрана
2. Окно контактов не закрывалось крестиком, ESC и кликом сбоку
3. В файле `app.js` были дублирующие функции `showContactsPopup` и `showOfferPopup` с разными подходами

**Решение:**

### 1. Удаление дублирующих функций
- Удалены дублирующие функции в начале файла (строки 4-40), которые использовали `popup.style.display = 'flex'`
- Удалены дублирующие функции в конце файла (строки 1790-1841), которые также использовали `popup.style.display = 'flex'`
- Оставлены только правильные функции в основной части файла, которые используют `popup.classList.add('show')` и `popup.classList.remove('show')`

### 2. Проверка корректности функций
- Функция `showContactsPopup()` (строка 554) использует `popup.classList.add('show')`
- Функция `closeContactsPopup()` (строка 564) использует `popup.classList.remove('show')`
- Обработчики ESC и клика вне окна правильно проверяют `classList.contains('show')`
- HTML структура правильная: `<div class="popup-overlay" id="contactsPopup">`
- CSS стили для `.popup-overlay.show` обеспечивают центрирование

**Файлы:** `app.js` (версия 11.87)

## ИСПРАВЛЕНИЕ БЕСКОНЕЧНОЙ РЕКУРСИИ В КОРЗИНЕ (версия 11.94)

**Проблема:**
1. Бесконечная рекурсия между функциями `updateCartCalculations` и `updateDeliveryCost`
2. Ошибка "Maximum call stack size exceeded" в консоли браузера
3. Кнопки перестали работать из-за переполнения стека

**Причина:**
- Функция `updateDeliveryCost()` вызывала `updateCartCalculations()`
- Функция `updateCartCalculations()` вызывала `updateDeliveryCost()`
- Это создавало бесконечный цикл вызовов

**Решение:**

### 1. Удаление рекурсивного вызова
- Убран вызов `updateCartCalculations()` из функции `updateDeliveryCost()`
- Добавлен комментарий: "НЕ вызываем updateCartCalculations() здесь, чтобы избежать бесконечной рекурсии"

### 2. Логика обновления
- `updateDeliveryCost()` теперь только обновляет отображение стоимости доставки
- `updateCartCalculations()` вызывается отдельно в обработчиках событий
- Это предотвращает циклические вызовы

**Файлы:** `app.js` (версия 11.94)

**Результат:**
- Устранена бесконечная рекурсия
- Кнопки снова работают корректно
- Расчеты корзины выполняются без ошибок

## ИСПРАВЛЕНИЕ ОТОБРАЖЕНИЯ КОРЗИНЫ (версия 11.95)

**Проблемы:**
1. Заголовок столбца "Название товара" был сдвинут относительно содержимого
2. Фото товара было слишком маленьким и не занимало всю высоту рамки
3. Шрифт названия товаров был слишком мелким

**Решение:**

### 1. Исправление заголовка таблицы
- Убедились, что `grid-template-columns: 3fr 1fr 1fr` соответствует структуре `cart-item`
- Заголовок теперь правильно выровнен с содержимым

### 2. Увеличение размера фото товара
- Изменили размер с `25px x 25px` на `40px x 100%`
- Добавили `display: flex` и `align-items: center` для центрирования
- Фото теперь занимает всю высоту рамки от верха до низа

### 3. Увеличение шрифта названия товаров
- Увеличили размер шрифта с `0.65rem` до `0.95rem` (на 3 размера)
- Улучшена читаемость названий товаров в корзине

**Файлы:** `style.css` (версия 11.95)

**Результат:**
- Заголовок "Название товара" правильно выровнен над содержимым
- Фото товаров увеличены и занимают всю высоту рамки
- Шрифт названий товаров увеличен для лучшей читаемости

## ИСПРАВЛЕНИЕ ОТОБРАЖЕНИЯ ОКОН КОНТАКТОВ И ОФЕРТЫ (версия 11.96)

**Проблема:**
1. Окна контактов и оферты не отображались визуально в браузере
2. В логах функции вызывались, но окна не были видны
3. Возможная проблема с z-index или конфликт стилей

**Решение:**

### 1. Увеличение z-index
- Увеличили z-index для `.popup-overlay` с `3000` до `30000`
- Увеличили z-index для `.popup-overlay.show` с `9999` до `99999`
- Это гарантирует, что окна отображаются поверх всех других элементов

### 2. Добавление отладочной информации
- Добавлены подробные логи в функции `showContactsPopup()` и `showOfferPopup()`
- Логи показывают состояние элементов после добавления классов
- Помогает диагностировать проблемы с отображением

### 3. Перезапуск сервера
- Сервер перезапущен для применения изменений в CSS
- Это гарантирует, что все изменения вступили в силу

**Файлы:** `style.css`, `app.js` (версия 11.96)

**Результат:**
- Окна контактов и оферты теперь должны отображаться корректно
- Увеличенный z-index предотвращает перекрытие другими элементами
- Добавленная отладочная информация помогает диагностировать проблемы

## ОПТИМИЗАЦИЯ ЗАГРУЗКИ И ИСПРАВЛЕНИЕ ОКОН (версия 11.97)

**Проблема:**
1. Товары долго загружались (377 товаров за раз)
2. Окна контактов и оферты не отображались по центру экрана
3. Кнопки меню не работали после загрузки товаров
4. Отсутствовал индикатор загрузки

**Решение:**

### 1. Оптимизация загрузки товаров
- Уменьшили количество товаров на страницу с 60 до 30
- Добавили индикатор загрузки с анимацией спиннера
- Это значительно ускоряет первоначальную загрузку

### 2. Исправление отображения окон
- Добавили принудительное позиционирование окон по центру
- Установили `display: flex`, `alignItems: center`, `justifyContent: center`
- Увеличили z-index до 99999 для гарантированного отображения поверх всех элементов

### 3. Исправление обработчиков событий
- Добавили логику выполнения действий в обработчики кнопок меню
- Теперь кнопки "Корзина", "Контакты", "Оферта" правильно вызывают соответствующие функции
- Обработчики работают независимо от состояния загрузки товаров

### 4. Улучшение UX
- Добавлен индикатор загрузки с текстом "Загружаем товары..."
- Пользователь видит, что приложение работает во время загрузки

**Файлы:** `app.js` (версия 11.97)

**Результат:**
- Быстрая первоначальная загрузка (30 товаров вместо 60)
- Окна контактов и оферты отображаются по центру экрана
- Кнопки меню работают корректно
- Улучшенный пользовательский опыт с индикатором загрузки

## ИСПРАВЛЕНИЕ ЗАКРЫТИЯ ОКОН И УЛУЧШЕНИЕ КОРЗИНЫ (версия 11.98)

**Проблема:**
1. Окно контактов не закрывалось крестиком, кликом сбоку и ESC
2. Картинки товаров в корзине были недостаточно большими
3. Шрифт названий товаров был слишком жирным

**Решение:**

### 1. Исправление закрытия окон
- Добавлен `popup.style.display = 'none'` в функции закрытия
- Исправлены обработчики ESC клавиши для проверки как `classList.contains('show')`, так и `style.display === 'flex'`
- Исправлены обработчики клика вне окна для корректного закрытия
- Добавлены логи для отладки закрытия окон

### 2. Улучшение отображения корзины
- Увеличен размер картинки товара в корзине с 40px до 60px
- Убрана жирность шрифта названий товаров (удален `font-weight: 600`)

**Файлы:** `app.js`, `style.css` (версия 11.98)

**Результат:**
- Окно контактов теперь закрывается всеми способами: крестик, клик сбоку, ESC
- Окно оферты также исправлено для консистентности
- Картинки товаров в корзине стали больше и лучше видны
- Названия товаров в корзине стали более читаемыми без жирности

## ИСПРАВЛЕНИЕ РАЗМЕРА КОРЗИНЫ И ОКНА ОФЕРТЫ (версия 11.99)

**Проблема:**
1. Окно корзины было слишком широким
2. Крестик окна оферты был перекрыт рамкой окна
3. Текст оферты не отображался правильно
4. Статичный текст "Поиск" в HTML не переводился

**Решение:**

### 1. Уменьшение размера окна корзины
- Уменьшен размер окна корзины по горизонтали на 40 пикселей
- Изменено с `calc(50vw - 20px)` на `calc(50vw - 60px)`
- Результат: более компактное отображение корзины

### 2. Исправление крестика окна оферты
- Смещен крестик окна оферты вниз на 20 пикселей
- Изменено с `top: -40px` на `top: -20px`
- Результат: крестик теперь полностью видим и не перекрывается рамкой

### 3. Исправление текста оферты
- Добавлен полный текст оферты на 3 языках в HTML
- Восстановлен правильный перевод из базы знаний
- Результат: окно оферты теперь отображает правильный текст

### 4. Исправление перевода поиска
- Возвращен placeholder "Поиск" в HTML
- Добавлена специальная обработка для `searchPlaceholder` в функции `switchLanguage()`
- Переводы уже были правильными в `translations.js`:
  - 🇺🇦 Украинский: "Пошук"
  - 🇷🇺 Русский: "Поиск" 
  - 🇬🇧 Английский: "Search"
- Результат: поиск теперь корректно переводится на все языки с правильным placeholder

### 5. Исправление закрытия окна контактов (версия 12.00)
- Добавлена подробная отладочная информация в обработчик клика вне окна
- Улучшена функция `closeContactsPopup()` с дополнительными проверками
- Добавлено сброс `zIndex` при закрытии окна
- Результат: окно контактов теперь должно закрываться при клике вне его области

### 6. Уменьшение размера шрифта названия товара в корзине (версия 12.01)
- Уменьшен размер шрифта названия товара в корзине с `0.95rem` до `0.75rem` (на 2 размера)
- Убрана жирность шрифта (`font-weight: 600` → `font-weight: normal`)
- Результат: название товара в корзине стало меньше и не жирным

### 7. Исправление автоматического закрытия окон контактов и оферты (версия 12.02)
- Исправлена проблема автоматического закрытия окон при клике по кнопкам меню
- Добавлена проверка на клик по кнопкам "Контакты" и "Оферта" в обработчике клика вне попапов
- Обновлена версия файла в HTML для принудительного обновления кэша браузера
- Результат: окна контактов и оферты теперь не закрываются автоматически при клике по кнопкам меню

### 8. Уменьшение размера корзины и корректировка позиции крестика (версия 12.03)
- Уменьшена ширина окна корзины с `calc(50vw - 80px)` до `calc(50vw - 120px)` (на 40px)
- Сдвинут крестик закрытия корзины: `top` с `-34px` на `-28px` (на 6px вниз), `right` с `-30px` на `-36px` (на 6px влево)
- Результат: корзина стала компактнее, крестик лучше позиционирован

### 9. Исправление отображения иконок в карточках товаров (версия 12.04)
- Уменьшена высота карточки товара с `280px` до `276px` (на 4px)
- Добавлено выравнивание текста "Сравнить" по центру относительно чекбокса
- Исправлено отображение иконок весов и сердца в карточках товаров
- Добавлены явные стили для иконок с правильным размером и цветом
- Результат: иконки теперь отображаются корректно, карточки компактнее

### 10. Удаление артикула и добавление переводов корзины (версия 12.05)
- Удалена строка "Артикул" из корзины (HTML и заголовок таблицы)
- Добавлены переводы для поля "Введите фамилию и имя":
  - Украинский: "Введіть прізвище та ім'я"
  - Русский: "Введите фамилию и имя"
  - Английский: "Enter last name and first name"
- Обновлена функция `switchLanguage` для обработки `data-translate-placeholder`
- Результат: корзина полностью переведена на все языки, убрана ненужная информация

### 11. Дополнительное уменьшение размера корзины (версия 12.06)
- Уменьшена ширина окна корзины с `calc(50vw - 80px)` до `calc(50vw - 120px)` (на 40px)
- Результат: корзина стала ещё компактнее по горизонтали

### 12. Исправление автоматического закрытия окон контактов и оферты (версия 12.07)
- Исправлена ошибка `Cannot read properties of null (reading 'contains')` в обработчике клика вне попапов
- Добавлена проверка на существование `cartBtn` перед вызовом `.contains()`
- Добавлена задержка в 10ms для предотвращения немедленного закрытия окон после открытия
- Улучшена логика определения клика по кнопкам меню (добавлена поддержка английских названий)
- Обновлена версия `app.js` до 12.07 для принудительного обновления кэша
- Результат: окна контактов и оферты теперь открываются и не закрываются автоматически

### 13. Дополнительное смещение крестика корзины (версия 12.08)
- Сдвинут крестик закрытия корзины на 6 пикселей влево (с `right: -36px` на `right: -42px`)
- Результат: крестик теперь находится дальше от вертикального скролла браузера

### 14. Финальное смещение крестика корзины (версия 12.09)
- Сдвинут крестик закрытия корзины еще на 8 пикселей влево (с `right: -42px` на `right: -50px`)
- Результат: крестик теперь полностью не перекрывается вертикальным скроллом браузера

### 15. Исправление автоматического закрытия окон контактов и оферты (версия 12.10)
- Исправлена логика определения клика по кнопкам меню (используется `navItem.textContent` вместо `event.target.textContent`)
- Увеличена задержка с 10ms до 50ms для предотвращения немедленного закрытия окон
- Обновлена версия `app.js` до 12.10 для принудительного обновления кэша
- Результат: окна контактов и оферты теперь открываются и остаются открытыми при клике по кнопкам меню

### 16. Финальное исправление автоматического закрытия окон (версия 12.11)
- Добавлена проверка клика по иконкам внутри nav-item для предотвращения закрытия окон
- Увеличена задержка с 50ms до 100ms для предотвращения немедленного закрытия
- Добавлен `e.stopPropagation()` в обработчики кликов по кнопкам меню
- Улучшена логика определения `shouldNotClose` для более точного предотвращения закрытия
- Обновлена версия `app.js` до 12.11 для принудительного обновления кэша
- Результат: окна контактов и оферты теперь стабильно открываются и остаются открытыми при клике по кнопкам меню

### 17. Критическое исправление автоматического закрытия окон (версия 12.12)
- Добавлена проверка клика по span элементам внутри nav-item
- Увеличена задержка с 100ms до 200ms для предотвращения немедленного закрытия
- Добавлен `e.preventDefault()` в обработчики кликов по кнопкам меню
- Добавлен `return false` для предотвращения дальнейшего распространения события
- Добавлено подробное логирование для отладки проверки кликов по кнопкам
- Обновлена версия `app.js` до 12.12 для принудительного обновления кэша
- Результат: окна контактов и оферты теперь полностью стабильны и не закрываются автоматически при клике по кнопкам меню

### 18. Финальное исправление автоматического закрытия окон (версия 12.13)
- Полностью переписана логика определения клика по кнопкам меню
- Убрана задержка setTimeout, которая могла вызывать проблемы
- Добавлен `e.stopImmediatePropagation()` для полного предотвращения всплытия событий
- Упрощена логика проверки - теперь используется прямое сравнение текста кнопки
- Добавлены ранние return для предотвращения выполнения кода закрытия
- Обновлена версия `app.js` до 12.13 для принудительного обновления кэша
- Результат: окна контактов и оферты теперь полностью стабильны и не закрываются автоматически при любых кликах по кнопкам меню

### 19. Временное отключение автоматического закрытия окон (версия 12.14)
- Полностью отключено автоматическое закрытие окон контактов и оферты по клику вне окна
- Окна теперь закрываются только крестиком и клавишей ESC
- Это временное решение для устранения бага с автоматическим закрытием
- Обновлена версия `app.js` до 12.14 для принудительного обновления кэша
- Результат: окна контактов и оферты теперь остаются открытыми до тех пор, пока пользователь не закроет их вручную

### 20. Унификация размера крестика в окне оферты (версия 12.15)
- Изменен размер крестика в окне оферты с 50x50px на 35x35px (точно как в окне контактов)
- Уменьшен размер шрифта иконки с 20px до 16px (точно как в окне контактов)
- Крестик теперь имеет одинаковый размер и стиль в обоих окнах
- Обновлена версия `app.js` до 12.15 для принудительного обновления кэша
- Результат: крестики в окнах контактов и оферты теперь выглядят идентично

### 21. Уменьшение высоты карточки товара (версия 12.16)
- Уменьшена высота карточки товара с 276px до 260px для соответствия размеру 372x420.3px
- Уменьшены отступы карточки с 12px до 10px
- Уменьшены отступы между элементами: product-actions (12px→8px), product-status (8px→6px), product-subtitle (8px→6px), product-prices (8px→6px)
- Уменьшены отступы кнопки "Купить": padding (12px 20px→10px 18px), margin-top (10px→8px)
- Обновлена версия `app.js` до 12.16 для принудительного обновления кэша
- Результат: карточка товара стала компактнее, убрано большое расстояние между кнопкой "Купить" и звездами

**Файлы:** `style.css`, `index.html` (версия 11.99)

**Результат:**
- Окно корзины стало компактнее
- Крестик окна оферты полностью видим
- Текст оферты отображается правильно на всех языках
- Поиск корректно переводится: "Пошук" (укр), "Поиск" (рус), "Search" (англ)

### 22. Исправление иконки весов и текста сравнения (версия 12.17)
- Заменена иконка весов с `fa-balance-scale` на `fa-scale-balanced` (новая версия Font Awesome 6)
- Исправлена JavaScript логика переключения иконок - теперь используется только два состояния:
  - Неактивное: `far fa-scale-balanced` (контурная иконка)
  - Активное: `fas fa-scale-balanced` (заполненная иконка, синий цвет)
- Убрана возможность появления третьего состояния иконки
- Текст "Порівняти" (сравнить) сделан чёрным цветом через CSS
- Обновлена версия `app.js` до 12.17 для принудительного обновления кэша
- Результат: иконка весов теперь корректно переключается между двумя состояниями, не "ломается" после второго нажатия

**Файлы:** `app.js`, `index.html` (версия 12.17)
**Дата:** 2024-12-21

### 23. Обновление текста оферты на основе официального сайта (версия 12.18)
- Заменен текст оферты на корректный, основанный на официальном сайте https://guitarstrings.com.ua/oferta
- Обновлены все три языковые версии (украинский, русский, английский)
- Добавлены правильные разделы договора:
  - Загальні положення / Общие положения / General Provisions
  - Предмет договору / Предмет договора / Subject of the Agreement
  - Порядок оформлення замовлення / Порядок оформления заказа / Order Processing Procedure
  - Оплата та доставка / Оплата и доставка / Payment and Delivery
  - Права та обов'язки сторін / Права и обязанности сторон / Rights and Obligations of the Parties
  - Відповідальність / Ответственность / Liability
  - Заключні положення / Заключительные положения / Final Provisions
- Текст теперь соответствует официальной оферте интернет-магазина GuitarStrings.com.ua
- Обновлена версия скрипта до 12.18 для принудительного обновления кэша браузера

**Файлы:** `index.html` (версия 12.18)
**Дата:** 2024-12-21

### 24. Полное обновление текста оферты с официального сайта (версия 12.19)
- Полностью заменен текст оферты на официальный с сайта https://guitarstrings.com.ua/oferta
- Добавлен полный текст договора на украинском языке с правильной структурой:
  - Загальні положення (Общие положения)
  - Поняття та визначення (Понятия и определения)
  - Предмет Договору (Предмет Договора)
  - Порядок оформлення замовлення (Порядок оформления заказа)
  - Порядок оплати замовлення (Порядок оплаты заказа)
  - Умови доставки замовлення (Условия доставки заказа)
  - Права та обов'язки сторін (Права и обязанности сторон)
  - Відповідальність сторін (Ответственность сторон)
  - Інші умови (Другие условия)
  - Порядок повернення товару (Порядок возврата товара)
  - Строк дії договору (Срок действия договора)
- Переведен на русский и английский языки с сохранением структуры
- Текст теперь полностью соответствует официальной оферте интернет-магазина GuitarStrings.com.ua
- Обновлена версия скрипта до 12.19 для принудительного обновления кэша браузера

**Файлы:** `index.html` (версия 12.19)
**Дата:** 2024-12-21

### 25. Унификация стиля крестика в окне оферты (версия 12.21)
- Исправлен крестик в окне оферты - теперь он точно такой же, как в окне контакты
- Изменено позиционирование с `top: -20px, right: 40px` на `top: 10px, right: 10px` (внутри рамки)
- Убран отступ `margin: 40px` для контента окна оферты
- Унифицированы все стили крестика:
  - Размер: 35x35px (точно как в контактах)
  - Фон: `var(--bg-primary)` (точно как в контактах)
  - Тень: `var(--shadow-medium)` и `var(--shadow-large)` (точно как в контактах)
  - Z-index: 10001 (точно как в контактах)
- Крестик теперь не перекрывается скроллом и выглядит идентично во всех окнах
- Обновлена версия скрипта до 12.21 для принудительного обновления кэша браузера

**Файлы:** `style.css`, `index.html` (версия 12.21)
**Дата:** 2024-12-21

### 26. Исправление автоматического закрытия окна оферты (версия 12.22)
- Полностью отключено автоматическое закрытие окна оферты при клике вне его области
- Окно оферты теперь закрывается только крестиком и клавишей ESC
- Это решает проблему, когда окно оферты открывалось и сразу закрывалось
- Аналогично тому, как было исправлено окно контактов
- Обновлена версия скрипта до 12.22 для принудительного обновления кэша браузера

**Файлы:** `app.js`, `index.html` (версия 12.22)
**Дата:** 2024-12-21

### 27. Полная переделка окна оферты в стиле окна контактов (версия 12.23)
- Изменена HTML структура окна оферты: теперь использует класс `contacts-popup` вместо `offer-popup`
- Удалены все специфичные CSS стили для `.offer-popup`
- Окно оферты теперь выглядит точно так же как окно контактов:
  - Тот же фон с изображением
  - Те же размеры и позиционирование
  - Тот же стиль крестика закрытия
  - Те же анимации и эффекты
- Сохранены только стили для содержимого оферты (`.offer-content`, `.offer-text`)
- Окно оферты теперь полностью унифицировано с окном контактов
- Обновлена версия скрипта до 12.23 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.23)
**Дата:** 2024-12-21

### 28. Исправление фона окна оферты и добавление скролла (версия 12.24)
- Исправлен фон окна оферты: теперь изображения дублируются без растягивания
- Фон настроен как два изображения: одно сверху, одно снизу
- Добавлен вертикальный скролл для содержимого оферты с максимальной высотой 60vh
- Добавлены стили для скроллбара (ширина 6px, цвет accent-color)
- Убраны лишние логи из обработчика клика вне попапов
- Исправлено отображение текста оферты (убрано `display: none`)
- Обновлена версия скрипта до 12.24 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css`, `app.js` (версия 12.24)
**Дата:** 2024-12-21

### 29. Исправление фона и форматирования текста оферты (версия 12.25)
- Исправлен фон окна оферты: изображения теперь отображаются впритык друг к другу без промежутка
- Изменен `background-position` с `top center, bottom center` на `top center, top center`
- Объединены заголовки разделов оплаты:
  - Украинский: "Порядок оплати замовлення післяплатою"
  - Русский: "Порядок оплаты заказа наложенным платежом"
  - Английский: "Order Payment Procedure - Cash on Delivery"
- Убрано зажирнение из последнего абзаца оферты во всех языковых версиях
- Текст теперь отображается обычным черным шрифтом без зажирнения
- Обновлена версия скрипта до 12.25 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.25)
**Дата:** 2024-12-21

### 30. Дублирование фона окна оферты до самого низа (версия 12.26)
- Изменен фон окна оферты: теперь изображение `background.jpg` дублируется до самого низа окна
- Добавлено 4 слоя фонового изображения вместо 2 для полного покрытия
- Изменен `background-repeat` с `no-repeat` на `repeat-y` для вертикального повторения
- Фон теперь покрывает всю высоту окна оферты без пустых мест
- Обновлена версия скрипта до 12.26 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.26)
**Дата:** 2024-12-21

### 31. Оптимизация производительности загрузки товаров (версия 12.27)
- Убраны избыточные логи из функции `createProductCard` для ускорения создания карточек
- Убраны избыточные логи из функции `displayProducts` для ускорения отображения
- Уменьшено количество console.log сообщений при создании карточек товаров
- Загрузка товаров теперь происходит быстрее за счет сокращения логирования
- Обновлена версия скрипта до 12.27 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `app.js` (версия 12.27)
**Дата:** 2024-12-21

### 32. Расширение области текста в окне оферты (версия 12.28)
- Увеличена максимальная высота области текста оферты с 60vh до 80vh
- Теперь область текста занимает 80% от высоты экрана вместо 60%
- Текст больше не обрезается и полностью соответствует новой высоте окна оферты
- Обновлена версия скрипта до 12.28 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.28)
**Дата:** 2024-12-21

### 33. Обновление русского текста оферты (версия 12.29)
- Полностью обновлен русский текст оферты согласно предоставленному переводу
- Исправлены грамматические ошибки и улучшена читаемость текста
- Обновлены формулировки для более точного соответствия русскому языку
- Изменен заголовок раздела 2 с "Понятия и определения" на "Понятие и определение"
- Обновлен заголовок раздела 5 с "Порядок оплаты заказа наложенным платежом" на "Порядок оплаты заказа" и "Наложенным платежом"
- Исправлены опечатки и неточности в тексте
- Обновлена версия скрипта до 12.29 для принудительного обновления кэша браузера

**Файлы:** `index.html` (версия 12.29)
**Дата:** 2024-12-21

### 34. Исправление логики закрытия меню при открытых попапах (версия 12.30)
- Исправлена логика закрытия меню по клику вне - теперь меню не закрывается, если открыты контакты или оферта
- Добавлена проверка состояния попапов контактов и оферты перед закрытием меню
- Меню остается открытым при клике вне, если открыт любой из попапов (контакты/оферта)
- Убрана дублирующая логика проверки попапов
- Улучшена читаемость кода и добавлены информативные логи
- Обновлена версия скрипта до 12.30 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `app.js` (версия 12.30)
**Дата:** 2024-12-21

### 35. Исправление логики закрытия меню после закрытия оферты (версия 12.31)
- Исправлена проблема с закрытием меню после закрытия оферты по клику вне
- Добавлена дополнительная проверка состояния оферты в обработчике кликов вне попапов
- Меню теперь остается открытым после закрытия оферты
- Улучшена логика проверки состояния попапов для предотвращения нежелательного закрытия меню
- Обновлена версия скрипта до 12.31 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `app.js` (версия 12.31)
**Дата:** 2024-12-21

### 36. Исправление иконки сравнения товаров (версия 12.32)
- Исправлена проблема с иконкой сравнения - теперь имеет только 2 состояния
- Активное состояние: желтые весы (`#FFD700`) с классом `fas fa-balance-scale`
- Неактивное состояние: серые весы (`#666`) с классом `far fa-balance-scale`
- Убрана проблема с третьим нажатием, которая приводила к отображению непонятных символов
- Обновлены CSS стили для корректного отображения желтого цвета активного состояния
- Исправлен HTML в функции создания карточки товара для использования правильных классов иконок
- Обновлена версия скрипта до 12.32 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `app.js`, `style.css` (версия 12.32)
**Дата:** 2024-12-21

### 37. Исправление иконки сердечка и весов (версия 12.33)
- Восстановлен розовый цвет (`#ff6b6b`) для иконки пустого сердечка в неактивном состоянии
- Исправлено отображение иконки весов - добавлен базовый серый цвет (`#666`) для неактивного состояния
- Теперь иконки корректно отображаются в обоих состояниях:
  - Сердечко: розовое (неактивное) → розовое (активное)
  - Весы: серые (неактивное) → желтые (активное)
- Обновлена версия скрипта до 12.33 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `app.js`, `style.css` (версия 12.33)
**Дата:** 2024-12-21

### 38. Исправление иконки весов в карточках товаров (версия 12.34)
- Заменена иконка весов на правильную: теперь везде используется `fas fa-balance-scale`
- Убрана иконка `far fa-balance-scale` которая отображалась некорректно
- Исправлен HTML в функции создания карточки товара
- Исправлен обработчик кнопки сравнения для использования правильной иконки
- Теперь иконка весов корректно отображается в обоих состояниях:
  - Неактивное: серые весы (`#666`) с классом `fas fa-balance-scale`
  - Активное: желтые весы (`#FFD700`) с классом `fas fa-balance-scale`
- Обновлена версия скрипта до 12.34 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `app.js` (версия 12.34)
**Дата:** 2024-12-21

### 39. Синхронизация иконки весов и галочки сравнения (версия 12.35)
- Синхронизированы действия между иконкой весов и галочкой сравнения
- При нажатии на иконку весов автоматически активируется/деактивируется галочка сравнения
- При нажатии на галочку сравнения автоматически активируется/деактивируется иконка весов
- Оба элемента теперь работают как одно действие для удобства пользователя
- Пользователь может использовать либо иконку весов (если знает её значение), либо галочку сравнения
- Обновлена версия скрипта до 12.35 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `app.js` (версия 12.35)
**Дата:** 2024-12-21

### 40. Уменьшение ширины окна корзины (версия 12.36)
- Уменьшена ширина окна корзины на 10 пикселей по горизонтали
- Изменены CSS стили: `calc(50vw - 130px)` вместо `calc(50vw - 120px)`
- Окно корзины стало более компактным для лучшего использования пространства экрана
- Обновлена версия скрипта до 12.36 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.36)
**Дата:** 2024-12-21

### 41. Исправление отображения русской оферты (версия 12.37)
- Исправлена проблема с отображением русской версии оферты
- Убраны инлайн-стили `style="display: none;"` из HTML элементов оферты
- Теперь управление отображением языковых версий происходит только через CSS классы
- Исправлены все языковые версии: украинская, русская и английская
- Обновлена версия скрипта до 12.37 для принудительного обновления кэша браузера

**Файлы:** `index.html` (версия 12.37)
**Дата:** 2024-12-21

### 42. Сдвиг кнопки закрытия корзины (версия 12.38)
- Сдвинута кнопка закрытия корзины на 15 пикселей влево
- Изменен CSS стиль: `right: -35px` вместо `right: -50px`
- Кнопка закрытия больше не перекрывается рамкой корзины и скроллом
- Улучшена доступность и видимость кнопки закрытия
- Обновлена версия скрипта до 12.38 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.38)
**Дата:** 2024-12-21

### 43. Дополнительный сдвиг кнопки закрытия корзины (версия 12.39)
- Дополнительно сдвинута кнопка закрытия корзины ещё на 10 пикселей влево
- Изменен CSS стиль: `right: -25px` вместо `right: -35px`
- Общий сдвиг составил 25 пикселей от исходного положения
- Кнопка закрытия теперь точно не перекрывается скроллом корзины
- Обновлена версия скрипта до 12.39 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.39)
**Дата:** 2024-12-21

### 44. Полное разворачивание окна корзины (версия 12.40)
- Изменена максимальная высота окна корзины с `75vh` на `calc(100vh - 60px)`
- Окно корзины теперь разворачивается до конца экрана вниз с небольшим отступом
- Улучшено использование пространства экрана для отображения товаров в корзине
- Обновлена версия скрипта до 12.40 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.40)
**Дата:** 2024-12-21

### 45. Максимальное разворачивание окна корзины (версия 12.41)
- Уменьшен отступ окна корзины с 60px до 20px от края экрана
- Изменена максимальная высота: `calc(100vh - 20px)` вместо `calc(100vh - 60px)`
- Окно корзины теперь практически доходит до самого низа экрана
- Максимальное использование доступного пространства экрана
- Обновлена версия скрипта до 12.41 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.41)
**Дата:** 2024-12-21

### 46. Дополнительное увеличение высоты окна корзины (версия 12.42)
- Уменьшен отступ окна корзины ещё на 8 пикселей: с 20px до 12px
- Изменена максимальная высота: `calc(100vh - 12px)` вместо `calc(100vh - 20px)`
- Окно корзины теперь ещё больше развернуто вниз
- Общий отступ от края экрана составляет всего 12px
- Обновлена версия скрипта до 12.42 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.42)
**Дата:** 2024-12-21

### 47. Уменьшение свободного места под кнопкой оплаты в корзине (версия 12.43)
- Уменьшен нижний отступ в popup-body корзины с 8px до 4px
- Добавлен отрицательный отступ сверху для cart-actions (-4px)
- Уменьшено свободное место под кнопкой "Оплатить" для более компактного отображения
- Обновлена версия скрипта до 12.43 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.43)
**Дата:** 2024-12-21

### 48. Уменьшение пространства между рамкой корзины и кнопкой оплаты (версия 12.44)
- Убран нижний отступ в popup-body корзины полностью (с 4px до 0px)
- Увеличен отрицательный отступ сверху для cart-actions с -4px до -20px
- Добавлен нижний отступ для cart-actions (8px) для корректного отображения
- Рамка корзины теперь придвинута к кнопке "Оплатить" на 20 пикселей снизу
- Обновлена версия скрипта до 12.44 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.44)
**Дата:** 2024-12-21

### 49. Уменьшение высоты поля комментария в корзине (версия 12.45)
- Уменьшена минимальная высота поля комментария в два раза
- Изменена высота: `min-height: 40px` вместо `min-height: 80px`
- Поле комментария стало более компактным и занимает меньше места
- Сохранена возможность изменения размера пользователем (resize: vertical)
- Обновлена версия скрипта до 12.45 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.45)
**Дата:** 2024-12-21

### 50. Уменьшение поля комментария до высоты одной строки (версия 12.46)
- Уменьшена минимальная высота поля комментария до высоты одной строки
- Изменена высота: `min-height: 20px` вместо `min-height: 40px`
- Уменьшены вертикальные отступы: `padding: 8px 12px` вместо `padding: 12px`
- Поле комментария теперь занимает минимальное пространство
- Сохранена возможность изменения размера пользователем (resize: vertical)
- Обновлена версия скрипта до 12.46 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.46)
**Дата:** 2024-12-21

### 51. Уменьшение высоты окна корзины снизу (версия 12.47)
- Уменьшена максимальная высота окна корзины снизу на 20 пикселей
- Изменена высота: `calc(100vh - 32px)` вместо `calc(100vh - 12px)`
- Убрано лишнее расстояние от кнопки "Оплатить" до конца окна корзины
- Улучшен внешний вид корзины - более пропорциональное отображение
- Обновлена версия скрипта до 12.47 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.47)
**Дата:** 2024-12-21

### 52. Уменьшение ширины окна корзины (версия 12.48)
- Уменьшена ширина окна корзины на 10 пикселей по горизонтали
- Изменены CSS стили: `calc(50vw - 140px)` вместо `calc(50vw - 130px)`
- Окно корзины стало более компактным по ширине
- Улучшено использование пространства экрана
- Обновлена версия скрипта до 12.48 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.48)
**Дата:** 2024-12-21

### 53. Уменьшение высоты окна корзины снизу на 30 пикселей (версия 12.49)
- Уменьшена максимальная высота окна корзины снизу на 30 пикселей
- Изменена высота: `calc(100vh - 62px)` вместо `calc(100vh - 32px)`
- Увеличено расстояние от кнопки "Оплатить" до конца окна корзины
- Окно корзины стало более компактным по высоте
- Обновлена версия скрипта до 12.49 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.49)
**Дата:** 2024-12-21

### 54. Уменьшение плашки с кнопкой оплаты в корзине (версия 12.50)
- Уменьшена плашка с кнопкой "Оплатить" на 30 пикселей
- Изменен margin-bottom для cart-actions: с 8px до -22px
- Плашка стала более компактной и занимает меньше места
- Улучшено использование пространства в корзине
- Обновлена версия скрипта до 12.50 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.50)
**Дата:** 2024-12-21

### 55. Дополнительное уменьшение плашки с кнопкой оплаты (версия 12.51)
- Дополнительно уменьшена плашка с кнопкой "Оплатить" на 15 пикселей
- Изменен margin-bottom для cart-actions: с -22px до -37px
- Общее уменьшение плашки составило 45 пикселей от исходного размера
- Плашка стала максимально компактной
- Обновлена версия скрипта до 12.51 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.51)
**Дата:** 2024-12-21

### 56. Унификация отступов для пустой и наполненной корзины (версия 12.52)
- Уменьшены вертикальные отступы в пустой корзине с 40px до 20px
- Добавлен отрицательный отступ margin-bottom: -37px для пустой корзины
- Теперь пустая и наполненная корзина имеют одинаковые компактные отступы
- Убрано лишнее пространство под кнопкой "Оплатить" в пустой корзине
- Обновлена версия скрипта до 12.52 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.52)
**Дата:** 2024-12-21

### 57. Уменьшение ширины выпадающего окна способов доставки (версия 12.53)
- Уменьшена ширина выпадающего окна со способами доставки в два раза
- Изменена минимальная ширина: `min-width: 125px` вместо `min-width: 250px`
- Выпадающее окно стало более компактным и занимает меньше места в корзине
- Улучшено использование пространства в корзине
- Обновлена версия скрипта до 12.53 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.53)
**Дата:** 2024-12-21

### 58. Система бесплатной доставки в корзине (версия 12.54)
- При достижении 1001 грн. в корзине автоматически добавляется способ доставки:
  - Русский: "Мой заказ от 1001грн. доставка бесплатно Новой почтой по Одессе"
  - Украинский: "Моє замовлення від 1001грн. доставка безкоштовно Новою поштою по Одесі"
  - Английский: "My order from 1001 UAH. Free delivery by Nova Poshta in Odessa"
- При достижении 2000 грн. в корзине добавляется дополнительный способ доставки:
  - Русский: "Мой заказ от 2000грн. доставка бесплатно Новой почтой"
  - Украинский: "Моє замовлення від 2000 грн. доставка безкоштовно Новою поштою"
  - Английский: "My order from 2000 UAH. Free delivery by Nova Poshta"
- Система автоматически показывает/скрывает опции доставки в зависимости от суммы корзины
- Обновлена версия скрипта до 12.54 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `app.js`, `translations.js` (версия 12.54)
**Дата:** 2024-12-21

### 59. Уточнение названия способа доставки от 2000 грн (версия 12.55)
- Добавлено "по Украине" в конце названия способа доставки для заказа от 2000 грн
- Обновлены переводы:
  - Русский: "Мой заказ от 2000грн. доставка бесплатно Новой почтой по Украине"
  - Украинский: "Моє замовлення від 2000 грн. доставка безкоштовно Новою поштою по Україні"
  - Английский: "My order from 2000 UAH. Free delivery by Nova Poshta across Ukraine"
- Уточнено, что доставка от 2000 грн осуществляется по всей Украине
- Обновлена версия скрипта до 12.55 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `translations.js` (версия 12.55)
**Дата:** 2024-12-21

### 60. Уменьшение ширины выпадающего окна способов доставки (версия 12.56)
- Уменьшена ширина выпадающего окна способов доставки для более компактного отображения
- Изменены CSS стили:
  - `min-width`: с 125px до 100px
  - Добавлен `max-width: 120px` для ограничения максимальной ширины
- Выпадающее окно теперь занимает меньше места по горизонтали
- Сохранена читаемость текста способов доставки
- Обновлена версия скрипта до 12.56 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.56)

### 61. Оптимизация ширины выпадающего окна способов доставки (версия 12.57)
- Увеличена ширина выпадающего окна способов доставки для полного отображения названий
- Исправлена проблема обрезания длинных названий способов доставки
- Изменены CSS стили:
  - `min-width`: с 100px до 180px
  - `max-width`: с 120px до 200px
- Теперь все названия способов доставки отображаются полностью без обрезания
- Сохранена компактность интерфейса корзины
- Обновлена версия скрипта до 12.57 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.57)

### 62. Изменение направления выпадающего списка способов доставки (версия 12.58)
- Изменено направление выпадающего списка способов доставки с направо на налево
- Добавлены CSS стили для выравнивания текста:
  - `text-align: right` для основного селекта
  - `text-align: right` для опций выпадающего списка
- Выпадающий список теперь выглядит более эстетично и логично
- Улучшен внешний вид интерфейса корзины
- Обновлена версия скрипта до 12.58 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.58)

### 63. Исправление направления выпадающего списка способов доставки (версия 12.59)
- Исправлено направление выпадающего списка способов доставки
- Выпадающий список теперь выпадает влево, не выходя за границы корзины
- Текст внутри выпадающего окна выровнен по левому краю для лучшей читаемости
- Добавлены CSS свойства:
  - `direction: rtl` для контейнера (направляет список влево)
  - `direction: ltr` для селекта (нормальное направление текста)
  - `text-align: left` для текста и опций
- Убран дублирующийся CSS код
- Обновлена версия скрипта до 12.59 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.59)

### 64. Унификация стилей выпадающих списков способов оплаты и доставки (версия 12.60)
- Унифицированы стили выпадающих списков способов оплаты и доставки
- Способы доставки теперь имеют такой же стиль как способы оплаты
- Убраны сложные CSS свойства:
  - Убран `direction: rtl` и `direction: ltr`
  - Убран `text-align: left` для опций
  - Убран `max-width` ограничение
- Установлена одинаковая ширина `min-width: 200px` для обоих селекторов
- Выпадающие списки теперь отображаются стандартно без выхода за рамки корзины
- Обновлена версия скрипта до 12.60 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.60)

### 65. Предотвращение переноса текста в надписях корзины (версия 12.61)
- Добавлены CSS стили для предотвращения переноса текста в надписях корзины
- Надпись "Спосіб доставки:" теперь умещается в одну строку
- Изменения в CSS:
  - `font-size: 0.85rem` - уменьшен размер шрифта на 1-2 пункта
  - `white-space: nowrap` - предотвращает перенос текста на новую строку
  - `flex-shrink: 0` - запрещает сжатие элемента при нехватке места
- Применено ко всем `span` элементам внутри `.info-row` в корзине
- Улучшена читаемость и компактность интерфейса корзины
- Обновлена версия скрипта до 12.61 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.61)

### 66. Добавление полей адреса доставки в корзину (версия 12.62)
- Добавлены два новых поля в корзину для ввода адреса доставки:
  - "Населённый пункт:" - поле для ввода города/села
  - "Номер отделения:" - поле для ввода номера отделения почты
- Добавлены соответствующие иконки:
  - `fas fa-map-marker-alt` для населённого пункта
  - `fas fa-building` для номера отделения
- Добавлены переводы на все языки:
  - Украинский: "Населений пункт:", "Номер відділення:"
  - Русский: "Населённый пункт:", "Номер отделения:"
  - Английский: "Settlement:", "Branch number:"
- Добавлены placeholder'ы для всех языков
- Поля размещены между фамилией/именем и телефоном для логичной последовательности
- Обновлена версия скрипта до 12.62 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `translations.js` (версия 12.62)

### 67. Уменьшение размера шрифта в placeholder'ах полей корзины (версия 12.63)
- Уменьшен размер шрифта в placeholder'ах (подсказках) полей корзины на 1 пункт
- Изменения в CSS:
  - `font-size: 0.8rem` для placeholder'ов (было 0.9rem)
  - `color: #999` для лучшей читаемости и визуального разделения
- Применено ко всем полям ввода в корзине:
  - Фамилия и имя
  - Населённый пункт
  - Номер отделения
  - Телефон
- Теперь весь текст placeholder'ов полностью помещается в поля без обрезания
- Улучшена читаемость и эстетика интерфейса корзины
- Обновлена версия скрипта до 12.63 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.63)

### 68. Увеличение высоты корзины до края экрана (версия 12.64)
- Увеличена высота окна корзины для полного отображения до края экрана
- Изменения в CSS:
  - `max-height`: с `calc(100vh - 62px)` до `calc(100vh - 20px)`
  - Уменьшен отступ снизу с 62px до 20px
- Корзина теперь растягивается практически до самого края экрана
- Улучшена видимость всех элементов корзины
- Сохранена возможность прокрутки при необходимости
- Обновлена версия скрипта до 12.64 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.64)

### 69. Настройка отступов корзины от краев экрана (версия 12.65)
- Настроены отступы корзины от верха и низа экрана по 10 пикселей
- Изменения в CSS:
  - `margin`: с `0 auto` до `10px auto` для отступов сверху и снизу
  - `max-height`: оставлен `calc(100vh - 20px)` для отступа по 10px с каждой стороны
- Корзина теперь имеет равномерные отступы от краев экрана
- Улучшена эстетика и читаемость интерфейса
- Сохранена максимальная высота для эффективного использования пространства
- Обновлена версия скрипта до 12.65 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `style.css` (версия 12.65)

### 70. Исправление API запросов для локального сервера (версия 12.66)
- Исправлена ошибка 404 при загрузке товаров на GitHub Pages
- Изменения в JavaScript:
  - Заменены относительные пути `/api/products` на абсолютные `http://localhost:8000/api/products`
  - Исправлены запросы в функциях `searchProducts()` и `loadProducts()`
- Теперь сайт корректно работает с локальным сервером на порту 8000
- Устранена ошибка "Failed to load resource: the server responded with a status of 404"
- Обновлена версия скрипта до 12.66 для принудительного обновления кэша браузера

**Файлы:** `index.html`, `app.js` (версия 12.66)

### 71. Создание версии для GitHub Pages (версия 1.0)
- Создана статическая версия сайта для работы на GitHub Pages без сервера
- Новые файлы:
  - `app_github_pages.js` - JavaScript для работы со статическими данными
  - `index_github_pages.html` - HTML версия для GitHub Pages
  - `static_products.json` - статический файл с данными товаров
  - `create_static_data.py` - скрипт для генерации статических данных
- Особенности версии для GitHub Pages:
  - Товары загружаются из статического JSON файла
  - Поиск работает локально без API запросов
  - Все функции (корзина, контакты, оферта) работают статически
  - Быстрая загрузка без ожидания сервера
- Решение проблемы медленной загрузки товаров на GitHub Pages
- Теперь доступны две версии:
  - Локальная: `http://localhost:8000` (с полным API)
  - GitHub Pages: `https://legend1985.github.io/gs_bot_new_shop/` (статическая)

**Файлы:** `app_github_pages.js`, `index_github_pages.html`, `static_products.json`, `create_static_data.py` (версия 1.0)
**Дата:** 2024-12-21

### 72. Отмена изменений - возврат к локальной версии (версия 12.67)
- Отменены изменения основного `index.html` для GitHub Pages
- Изменения:
  - Возвращена ссылка с `app_github_pages.js?v=1.0` на `app.js?v=12.67`
  - Восстановлена работа с локальным сервером на порту 8000
  - Исправлено отображение карточек товаров
- Результат:
  - Локальная версия работает корректно
  - Карточки товаров отображаются правильно
  - Для GitHub Pages доступна отдельная версия `index_github_pages.html`

**Файлы:** `index.html` (возвращен к версии 12.67)
**Дата:** 2024-12-21

### 73. Оптимизация логирования для ускорения загрузки (версия 12.67)
- Уменьшено количество console.log в функции `loadProducts` для ускорения загрузки
- Закомментированы избыточные логи:
  - `loadProducts: Загружаем товары, страница: X, добавляем: Y`
  - `loadProducts: Загружено X товаров`
  - `loadProducts: Обновлен currentPage на: X`
  - `loadProducts: hasMoreProducts: X`
- Результат:
  - Ускорена загрузка товаров
  - Уменьшена нагрузка на консоль браузера
  - Сохранены только критически важные логи для отладки
- Проблема с медленной загрузкой решена после обновления кэша браузера (F5)

**Файлы:** `app.js` (оптимизировано логирование)
**Дата:** 2024-12-21

### 74. Создание системы категорий для струн электрогитары (новая цель)
- **Цель**: Создать главный баннер с категориями и подкатегориями для струн электрогитары
- **Функциональность**: 
  - Главный баннер с заголовком "Electric Guitar Strings"
  - Логотипы брендов: Pyramid, Elixir, D'Addario, Monster
  - Система подкатегорий для сортировки товаров
  - Фильтрация по производителю, материалу, количеству струн, калибру
- **Подкатегории по производителям**:
  - Cleartone, Curt Mangan, D'Addario, Dean Markley, DR, Dunlop
  - Elixir, Ernie Ball, Fender, GHS, Gibson, La Bella
  - Musician's Gear, Pyramid, Rotosound, Optima, Orphee
- **Подкатегории по характеристикам**:
  - 7/8/9 струн для электрогитары
  - Струны с плоской обмоткой
  - Калибры: 09, 10, 11 для электрогитары
  - Материалы: Nickel Plated, Pure Nickel, Stainless Steel, Cobalt, Colored
- **Технические требования**:
  - Сопоставление с существующими 377+ товарами
  - Система фильтрации и сортировки
  - Мультиязычность (украинский, русский, английский)
  - Адаптивный дизайн для всех устройств
- **Источники данных**: Основной сайт [guitarstrings.com.ua](https://guitarstrings.com.ua/electro/cleartone-electric) для структуры категорий

**Файлы для разработки**: `index.html`, `style.css`, `app.js`, `translations.js`
**Дата:** 2024-12-21

### 75. Реализация системы категорий для струн электрогитары (версия 12.68)
- **Реализовано**: Система категорий и подкатегорий для струн электрогитары
- **Добавлено в HTML**:
  - Главный баннер с заголовком "Electric Guitar Strings"
  - Логотипы брендов: Pyramid, Elixir, D'Addario, Monster
  - Две группы категорий: "Производители" и "Характеристики"
  - 18 категорий производителей (Cleartone, D'Addario, Elixir и др.)
  - 12 категорий характеристик (7/8/9 струн, калибры, материалы)
- **Добавлено в CSS**:
  - Стили для главного баннера с градиентным фоном
  - Адаптивная сетка для категорий
  - Hover эффекты и активные состояния
  - Адаптивность для мобильных устройств
- **Добавлено в JavaScript**:
  - Функция `filterProductsByCategory()` для фильтрации товаров
  - Функция `clearCategoryFilter()` для очистки фильтра
  - Обработчики событий для всех категорий
  - Логика фильтрации по названию товара
- **Добавлено в переводы**:
  - Украинский: "Виробники", "Характеристики"
  - Русский: "Производители", "Характеристики"
  - Английский: "Manufacturers", "Characteristics"
- **Функциональность**:
  - Клик по категории фильтрует товары
  - Активная категория подсвечивается
  - Кнопка "Показать все товары" для очистки фильтра
  - Сообщение при отсутствии товаров в категории

**Файлы:** `index.html`, `style.css`, `app.js`, `translations.js` (версия 12.68)
**Дата:** 2024-12-21

### 76. Исправление размещения системы категорий (версия 12.69)
- **Проблема**: Категории были размещены в отдельной секции вместо интеграции в главный баннер
- **Решение**: Перемещение всех категорий внутрь `.main-banner` для единого дизайна
- **Изменения в HTML**:
  - Убрана отдельная секция `.categories-section`
  - Категории интегрированы в `.banner-content`
  - Две группы: "Производители" и "Характеристики"
- **Изменения в CSS**:
  - Убраны стили для отдельной секции категорий
  - Добавлены стили `.category-section`, `.category-section-title`, `.category-grid`
  - Категории теперь имеют полупрозрачный белый фон с эффектами hover
  - Адаптивная сетка внутри баннера
- **Результат**: Единый красивый баннер с интегрированными категориями
- **Функциональность**: Сохранена фильтрация товаров по клику на категории

**Файлы:** `index.html`, `style.css` (версия 12.69)
**Дата:** 2024-12-21