# 🛠️ **Менеджер локального сервиса**

**Файл:** `service_manager.bat`

## 📋 **Описание**

Удобный BAT скрипт для управления локальными серверами проекта GS Bot New Shop. Предоставляет интуитивное меню для запуска, остановки и перезапуска **обоих серверов одновременно** или **каждого по отдельности**.

## 🏗️ **Архитектура серверов**

Проект поддерживает **две архитектуры серверов**:

### **Python стек (рекомендуемый):**
| Сервер | Функция | Порт | Файл | Управление |
|--------|---------|------|------|------------|
| **Python API** | Flask API (товары, пользователи, заказы) | 5000 | `api_server.py` | Опция [6] |
| **Python Web** | HTTP сервер + API прокси | 8000 | `server.py` | Опция [7] |

### **Node.js стек (альтернативный):**
| Сервер | Функция | Порт | Файл | Управление |
|--------|---------|------|------|------------|
| **Node.js Server** | Express + автоматический скрапинг | 8000 | `server.js` | Опция [8] |

## 🚀 **Функции**

### **Выбор типа сервера:**
- 🐍 **[1] Запустить Python сервисы** - server.py (порт 8000) + api_server.py (порт 5000)
- 🟨 **[2] Запустить Node.js сервис** - server.js (порт 8000) с автоматическим скрапингом

### **Управление запущенными сервисами:**
- 🛑 **Остановить все сервисы** - корректное завершение всех процессов Python/Node.js
- 📊 **Проверить статус сервисов** - мониторинг процессов и доступности портов
- 🌐 **Открыть сайт в браузере** - автоматическое открытие главной страницы

### **Индивидуальное управление:**
- 🎛️ **Управление Python API сервером** - api_server.py (порт 5000)
- 🌐 **Управление Python веб сервером** - server.py (порт 8000)
- 🟨 **Управление Node.js сервером** - server.js (порт 8000 с скрапингом)

### **Функции отдельных меню:**
- **Запуск** - проверка и запуск конкретного сервера
- **Остановка** - корректное завершение конкретного сервера
- **Перезапуск** - остановка и запуск конкретного сервера
- **Статус** - проверка процесса и доступности конкретного сервера

## 🎯 **Как использовать**

### **Запуск скрипта:**
```cmd
# Двойной клик на файле service_manager.bat
# ИЛИ из командной строки:
service_manager.bat
```

### **Главное меню:**
```
===========================================
     МЕНЕДЖЕР ЛОКАЛЬНОГО СЕРВИСА
===========================================

Проект: GS Bot New Shop

[1] Запустить Python сервисы (server.py + api_server.py)
[2] Запустить Node.js сервис (server.js)
[3] Остановить все сервисы
[4] Проверить статус сервисов
[5] Открыть сайт в браузере
[6] Управление Python API сервером
[7] Управление Python веб сервером
[8] Управление Node.js сервером
[9] Выход
```

### **Навигация:**
1. Выберите нужное действие (1-9)
2. Для быстрого старта используйте опции 1 или 2
3. Для индивидуального управления серверами используйте опции 6-8
4. Следуйте инструкциям на экране
5. Для возврата в меню нажмите любую клавишу

## 📊 **Что делает каждая опция**

### **[1] Запустить Python сервисы**
- ✅ Переходит в директорию проекта
- ✅ Останавливает старые процессы Python/Node.js
- ✅ Запускает Python API сервер (`api_server.py`) на порту 5000
- ✅ Запускает Python веб сервер (`server.py`) на порту 8000
- ✅ Показывает адреса доступа обоих серверов

### **[2] Запустить Node.js сервис**
- ✅ Проверяет наличие Node.js
- ✅ Останавливает старые процессы Python/Node.js
- ✅ Запускает Node.js сервер (`server.js`) на порту 8000
- ✅ Автоматически запускает скрапинг товаров
- ✅ Показывает адрес доступа сервера

### **[3] Остановить все сервисы**
- ✅ Ищет и завершает все процессы `python.exe` и `node.exe`
- ✅ Показывает результат операции

### **[4] Проверить статус сервисов**
- ✅ Показывает список активных процессов Python
- ✅ Показывает список активных процессов Node.js
- ✅ Проверяет доступность API сервера (порт 5000)
- ✅ Проверяет доступность веб сервера (порт 8000)
- ✅ Возвращает HTTP статус коды

### **[5] Открыть сайт в браузере**
- ✅ Автоматически открывает `http://localhost:8000/index.html`
- ✅ Использует системный браузер по умолчанию

### **[6] Управление Python API сервером**
- 🎛️ Открывает подменю для управления Flask API сервером:
  - Запуск API сервера (порт 5000)
  - Остановка API сервера
  - Перезапуск API сервера
  - Проверка статуса API сервера

### **[7] Управление Python веб сервером**
- 🌐 Открывает подменю для управления Python HTTP сервером:
  - Запуск веб сервера (порт 8000)
  - Остановка веб сервера
  - Перезапуск веб сервера
  - Проверка статуса веб сервера

### **[8] Управление Node.js сервером**
- 🟨 Открывает подменю для управления Node.js Express сервером:
  - Запуск Node.js сервера (порт 8000 с скрапингом)
  - Остановка Node.js сервера
  - Перезапуск Node.js сервера
  - Проверка статуса Node.js сервера

### **[9] Выход**
- ✅ Корректно завершает работу скрипта

## 🔧 **Технические детали**

### **Используемые команды:**
- `taskkill` - для остановки процессов (python.exe, node.exe)
- `tasklist` - для проверки процессов
- `curl` - для проверки доступности серверов
- `start` - для запуска серверов в новых окнах
- `node` - для проверки наличия Node.js

### **Порты:**
- **API сервер:** `http://localhost:5000` (api_server.py)
- **Веб сервер:** `http://localhost:8000` (server.py или server.js)

### **Процессы:**
- Управляет процессами `python.exe` и `node.exe`
- Безопасно завершает работу серверов
- Предотвращает конфликты портов
- Проверяет уникальность окон по заголовкам

## 💡 **Выбор между Python и Node.js**

### **🐍 Python стек (рекомендуемый):**
- ✅ **Надежность** - проверенная архитектура
- ✅ **Полный функционал** - API + статические файлы + проксирование
- ✅ **Умное проксирование** - `/api/*` автоматически перенаправляется на API сервер
- ✅ **Отдельные порты** - API (5000) + Web (8000)
- ✅ **Модульное управление** - можно перезапускать отдельно
- 🔄 **Ручной скрапинг** - требуется отдельный запуск

### **🟨 Node.js стек (простой):**
- ✅ **Автоматический скрапинг** - данные загружаются при старте
- ✅ **Один порт** - все на 8000
- ✅ **Быстрый старт** - один сервер вместо двух
- ⚠️ **Требует Node.js** - дополнительная установка
- ⚠️ **Менее гибкий** - перезапуск влияет на все

## 💡 **Советы по использованию**

### **Для разработки:**
1. Используйте **Python стек** для полного контроля
2. Выбирайте **Node.js** для быстрого тестирования с данными
3. **Проверка статуса** поможет убедиться, что сервер работает
4. **Открытие в браузере** для быстрого тестирования

### **При проблемах:**
1. Сначала **Остановите все сервисы** (опция 3)
2. Проверьте, что порты 5000 и 8000 свободны
3. **Запустите нужный стек** заново

### **Горячие клавиши:**
- `Win + R` → `cmd` → `service_manager.bat` - быстрый запуск

## 🎯 **Рекомендуемые рабочие процессы**

### **🐍 Для Python стека (рекомендуемый):**
```
1. Откройте service_manager.bat
2. Выберите [1] - Запустить Python сервисы
3. Выберите [5] - Открыть в браузере
4. Работайте с сайтом (API автоматически проксируется через порт 8000)
5. После изменений в API: [6] → [3] - Перезапустить API
6. После изменений в HTML/JS: [7] → [3] - Перезапустить веб
7. Для завершения выберите [3] - Остановить все сервисы
```

### **🟨 Для Node.js стека (быстрый старт):**
```
1. Откройте service_manager.bat
2. Выберите [2] - Запустить Node.js сервис
3. Выберите [5] - Открыть в браузере
4. Работайте с сайтом (все на 8000, данные предзагружены)
5. После изменений выберите [8] → [3] - Перезапустить сервер
6. Для завершения выберите [3] - Остановить все сервисы
```

## ⚠️ **Важные замечания**

### **Требования:**
- **Python стек:** установлен Python с Flask и другими зависимостями
- **Node.js стек:** установлен Node.js (скачать с nodejs.org)
- Скрипт работает только в директории проекта

### **Порты:**
- Порт 8000 должен быть свободен
- Для Python стека также требуется порт 5000
- Скрипт автоматически проверяет и освобождает порты

### **Безопасность:**
- Используйте с правами администратора при необходимости
- Node.js сервер требует установленного Node.js
- Python стек работает без дополнительных установок

---

**🚀 Удобное управление локальным сервисом в одном скрипте!**
